const fs=require('fs');const path=require('path');const {spawnSync}=require('child_process');const root=path.resolve(__dirname,'..');const manifest=JSON.parse(fs.readFileSync(path.join(root,'component.json'),'utf8'));const dist=path.join(root,'dist');const packageRoot=path.join(dist,'component');fs.rmSync(packageRoot,{recursive:true,force:true});fs.mkdirSync(packageRoot,{recursive:true});for(const name of ['component.json','service.cjs','README.md','ui'])fs.cpSync(path.join(root,name),path.join(packageRoot,name),{recursive:true});for(const file of manifest.requiredFiles)if(!fs.statSync(path.join(packageRoot,file),{throwIfNoEntry:false})?.isFile())throw new Error(`Missing ${file}`);const archive=path.join(dist,`PhotoFlow-${manifest.id}-${manifest.version}-${process.platform}-${process.arch}.zip`);fs.rmSync(archive,{force:true});const quote=value=>`'${String(value).replace(/'/g,"''")}'`;const result=process.platform==='win32'?spawnSync('powershell.exe',['-NoProfile','-Command',`Compress-Archive -LiteralPath ${quote(packageRoot)} -DestinationPath ${quote(archive)} -CompressionLevel Optimal -Force`],{stdio:'inherit'}):spawnSync('python3',['-c','import pathlib,sys,zipfile\ns,t=pathlib.Path(sys.argv[1]),pathlib.Path(sys.argv[2])\nwith zipfile.ZipFile(t,"w",zipfile.ZIP_DEFLATED) as z:\n for p in sorted(s.rglob("*")):\n  if p.is_file(): z.write(p,pathlib.Path(s.name)/p.relative_to(s))',packageRoot,archive],{stdio:'inherit'});if(result.error)throw result.error;if(result.status!==0)throw new Error(`Archive failed: ${result.status}`);console.log(`Installable component package: ${archive}`);
