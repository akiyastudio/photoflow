const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');const manifest=JSON.parse(fs.readFileSync(path.join(root,'component.json'),'utf8'));for(const file of manifest.requiredFiles)if(!fs.statSync(path.join(root,file),{throwIfNoEntry:false})?.isFile())throw new Error(`Missing ${file}`);if(manifest.componentHost.contributions.filter(item=>item.type==='component.sidePanel'&&item.placement==='workspace.videoTools').length!==2)throw new Error('Video tools must contribute two grouped panels');console.log('Video tools component layout passed');
