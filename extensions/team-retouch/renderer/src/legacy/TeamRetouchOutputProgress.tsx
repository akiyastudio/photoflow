import { useTeamOutputProgress } from './useTeamOutputProgress';

export const TeamOutputProgressPicker = ({ controller, disabled }: { controller: ReturnType<typeof useTeamOutputProgress>; disabled: boolean }) => <div className="flex min-w-0 flex-col items-center gap-2 text-xs text-slate-500"><span className="shrink-0 font-bold text-slate-600">合并保存到</span>{controller.hasProjectProgress ? <div className="flex min-w-0 items-center gap-2"><select aria-label="合并结果目标进度" value={controller.targetProgressId} disabled={disabled} onChange={event => controller.setTargetProgressId(event.target.value)} className="min-w-48 max-w-72 rounded-md border border-slate-200 bg-white px-2.5 py-2 text-xs font-bold text-slate-700 disabled:opacity-50"><option value="__new__">新建下一图片进度</option>{controller.progressOptions.map(option => <option key={option.folder.id} value={option.folder.id} disabled={option.disabled}>V{option.folder.versionKey} · {option.folder.displayName}{option.reason ? `（${option.reason}）` : ''}</option>)}</select><span className="hidden max-w-64 truncate text-slate-400 xl:inline">仅显示合法图片主进度</span></div> : <span className="rounded-md border border-slate-200 bg-white px-3 py-2 font-bold text-slate-700">项目根目录 / 团片协作合并</span>}</div>;
