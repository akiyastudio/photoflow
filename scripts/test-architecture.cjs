const assert = require('assert');
const fs = require('fs');
const path = require('path');
const { createEventBus } = require('../electron/services/event-bus.cjs');
const { createBackgroundTaskService } = require('../electron/services/background-task-service.cjs');
const { PLUGIN_DEFINITIONS, findPluginByCapability } = require('../electron/plugins/plugin-catalog.cjs');

const root = path.resolve(__dirname, '..');
const read = relativePath => fs.readFileSync(path.join(root, relativePath), 'utf8');
const lines = value => value.split(/\r?\n/).length;

const main = read('electron/main.cjs');
const preload = read('electron/preload.cjs');
const appEntry = read('src/App.tsx');
const app = appEntry + read('src/features/app/app-config.ts');
const globalStyles = read('src/index.css');
assert(appEntry.includes('className="app-titlebar relative z-50 flex h-10') && globalStyles.includes(':root { --app-titlebar-height: 2.5rem; }') && globalStyles.includes('.fixed.inset-0') && globalStyles.includes('top: var(--app-titlebar-height);') && globalStyles.includes('height: auto;'), 'all full-viewport renderer overlays must preserve the persistent application titlebar');
const titlebarTabOrder = read('src/features/app/useTitlebarTabOrder.ts');
const folderTabNavigation = read('src/features/app/useFolderTabNavigation.ts');
const toolViews = read('src/features/tools/ToolViews.tsx');
assert(toolViews.includes('图片来源文件夹') && toolViews.includes('视频来源文件夹') && toolViews.includes("resolveFilenameSelectionSource(sourceFolders, config.imageSourceFolderName, 'raw')") && toolViews.includes("resolveFilenameSelectionSource(sourceFolders, config.videoSourceFolderName, 'mov')"), 'filename selection must retain separate image/RAW and video/MOV source selectors with RAW and MOV defaults');
assert(toolViews.includes("open ? 'overflow-visible' : 'overflow-hidden'") && toolViews.includes('drivePickerOpen && <div role="menu"') && toolViews.includes("open ? 'rounded-t-[11px]' : ''") && toolViews.includes('rounded-b-[11px] border-t'), 'expanded home panels must keep their rounded background corners without clipping the SD drive picker menu');
assert(toolViews.includes("progressEvent.phase === 'copying'") && toolViews.includes('statusMessage={statusMsg}') && toolViews.includes('正在复制${label'), 'filename selection must show the current copied file in the progress status row instead of letting a target log hide it');
const importCompletionModel = read('src/features/tools/import-completion-model.ts');
const startupSdAutoImportHook = read('src/features/tools/use-startup-sd-auto-import.ts');
const startupSdAutoImportModel = read('src/features/tools/startup-sd-auto-import-model.ts');
const sdStartupImportModel = read('src/features/tools/sd-startup-import-model.ts');
const storageDeviceInventoryModel = read('src/features/tools/storage-device-inventory-model.ts');
const classifyImport = read('python/classify.py');
assert(toolViews.includes('getWorkspaceProjects(resolvedDestinationPath)') && toolViews.includes('legacyProject?.id === project.id') && toolViews.includes('legacyPathName') && toolViews.includes('value={project.id}') && classifyImport.includes("exact[0].get('id') or exact[0].get('path', '')"), 'SD import routing must resolve stable project identities and legacy moved paths to current category paths before committing files');
const ffmpegTranscode = read('python/ffmpeg_transcode.py');
const cutVideo = read('python/cut_video.py');
const videoTrimCommitService = read('electron/services/video-trim-commit-service.cjs');
const componentBuilder = read('scripts/build-components.cjs');
const rawDecoder = read('python/raw_decoder.py');
assert(!componentBuilder.includes("'research-tools':") && !componentBuilder.includes("'office-media-extractor':") && !fs.existsSync(path.join(root, 'extensions', 'research-tools')) && !fs.existsSync(path.join(root, 'extensions', 'office-media-extractor')), 'built-in research and Office workers must not retain retired optional-component packaging');
assert(toolViews.includes('const dateStr = `${month}.${day}`') && toolViews.includes('该月份中不存在这个日期') && toolViews.includes('min="1" max="12"'), 'birthday editor must save canonical dates and reject invalid or negative values');
const projectWorkspace = read('src/features/workspace/ProjectWorkspace.tsx') + read('src/features/workspace/ProjectToolModal.tsx') + read('src/features/workspace/ProjectWorkspaceLayout.tsx') + read('src/features/workspace/useProjectThumbnail.ts');
const projectWorkspaceContract = read('src/contracts/project-workspace-api.ts');
const projectWorkspaceClient = read('src/platform/project-workspace-client.ts');
const pluginContributions = read('src/features/plugins/plugin-contributions.ts');
const fileEntryInteractionModel = read('src/features/workspace/file-entry-interaction-model.ts');
const fileEntrySortModel = read('src/features/workspace/file-entry-sort-model.ts');
const folderAlphabetFilterModel = read('src/features/workspace/folder-alphabet-filter-model.ts');
const types = read('src/types.ts');
const marqueeSelectionModel = read('src/features/workspace/marquee-selection-model.ts');
const marqueeAutoScroll = read('src/features/workspace/marquee-auto-scroll.ts');
const projectVersionTree = read('src/components/ProjectVersionTree.tsx');
const versionTreeCanvas = read('src/features/versioning/use-version-tree-canvas.ts');
const browserContext = read('src/features/file-browser/browser-context.ts');
const inspirationLibrary = read('src/features/inspiration/InspirationLibrary.tsx');
const projectNavigator = read('src/components/ProjectNavigator.tsx');
const importSourceControls = read('src/components/ImportSourceControls.tsx');
const versionProgressPanel = read('src/features/versioning/VersionProgressPanel.tsx');
const trackingConfirmationPanel = read('src/features/versioning/TrackingConfirmationPanel.tsx');
const fileRootWatcherService = read('electron/services/file-root-watcher-service.cjs');
const workspaceTabModel = read('src/features/app/workspace-tab-model.ts');
const projectWorkspaceLifecycle = read('src/features/workspace/project-workspace-lifecycle.ts');
const workspaceTabsHook = read('src/features/app/useWorkspaceTabs.ts');
const settingsFeature = read('src/features/settings/SettingsFeature.tsx');
const usagePreferencesOnboarding = read('src/features/settings/UsagePreferencesOnboarding.tsx');
const storageUsageService = read('electron/services/storage-usage-service.cjs');
const storageDeviceService = read('electron/services/storage-device-service.cjs');
const appChrome = read('src/features/app/AppChrome.tsx');
const appErrorBoundary = read('src/features/app/AppErrorBoundary.tsx');
const workspaceDb = read('python/workspace_db.py');
const thumbnailDb = read('python/thumbnail_db.py');
const thumbnailPipeline = read('electron/thumbnail-pipeline.cjs');
const thumbnailService = read('electron/services/thumbnail-service.cjs');
const mediaCacheNamespace = read('electron/services/media-cache-namespace.cjs');
const recycleBinFailure = read('src/utils/recycleBinFailure.ts');
const recycleBinService = read('electron/services/recycle-bin-service.cjs');
assert(main.includes("mainWindow.webContents.on('before-input-event'") && main.includes("input.key !== 'F11'") && main.includes('mainWindow.setFullScreen(!mainWindow.isFullScreen())'), 'F11 must toggle the main application window between fullscreen and its previous window state');
const filesIpc = read('electron/modules/files-ipc.cjs');
const fileTransferService = read('electron/services/file-transfer-service.cjs');
const projectFileTaskService = read('electron/services/project-file-task-service.cjs');
const fileClipboardService = read('electron/services/file-clipboard-service.cjs');
const fileClipboardNative = read('electron/native/FileClipboardService.cs');
const fileClipboardBuilder = read('scripts/build-file-clipboard-service.cjs');
const workspaceIpc = read('electron/modules/workspace-ipc.cjs');
const workspaceRepository = read('electron/repositories/workspace-repository.cjs');
const workspaceService = read('electron/services/workspace-service.cjs');
const workspaceCatalogClient = read('src/platform/workspace-catalog-client.ts');
const importRecovery = read('electron/modules/workspace/import-recovery.cjs');
const projectVirtualPathService = read('electron/services/project-virtual-path-service.cjs');
const mediaIpc = read('electron/modules/media-ipc.cjs');
const brollImport = read('electron/modules/broll-import.cjs');
const versionsIpc = read('electron/modules/versions-ipc.cjs');
const versionTrackingIpc = read('electron/modules/version-tracking-ipc.cjs');
const selectionIpc = read('electron/modules/selection-ipc.cjs');
const selectionService = read('electron/services/selection-service.cjs');
const systemIpc = read('electron/modules/system-ipc.cjs');
const backupService = read('electron/services/backup-service.cjs');
const backupDb = read('python/backup_db.py');
const domainRecovery = read('python/domain_recovery.py');
const teamRetouchStorage = read('python/team_retouch_storage.py');
const teamRetouchDb = read('python/team_retouch_db.py');
const mediaRatingService = read('electron/services/media-rating-service.cjs');
const mediaRatingIpc = read('electron/modules/media-rating-ipc.cjs');
const imageThumbnailRuntime = read('electron/services/image-thumbnail-runtime.cjs');
const retiredCacheService = read('electron/services/retired-cache-service.cjs');
assert((projectWorkspace.match(/loadFinalVersionSummary\(\)/g) || []).length === 2, 'full-project media rating scans must only run during the explicit favorite export workflow');
assert(main.includes('const mediaInteractionDatabase = new PythonDatabaseClient') && main.includes('decideTrackingItem: mediaInteractionRepository.decideTrackingItem') && main.includes('const teamRetouchDatabase = new PythonDatabaseClient') && main.includes("processId: 'python:team-retouch-database'") && main.includes('...teamRetouchRepository'), 'tracking and team confirmations must use separate workers isolated from long media jobs and from each other');
assert(trackingConfirmationPanel.indexOf('setView(current => applyTrackingItemDecision') < trackingConfirmationPanel.indexOf('await window.electronAPI.decideProgressTrackingItem'), 'tracking decisions must update the UI optimistically before waiting for database persistence');
assert(projectWorkspace.indexOf('setPreviewRating(requestedRating)') < projectWorkspace.indexOf('await projectWorkspaceClient.setMediaRating'), 'media ratings must update the UI optimistically before metadata persistence');
assert(mediaRatingService.includes('void versionService.refreshMetadataFingerprint') && mediaRatingService.includes('return rating;'), 'rating fingerprint maintenance must run outside the interactive write critical path');
assert(settingsFeature.indexOf("id: 'general'") < settingsFeature.indexOf("id: 'project'") && settingsFeature.indexOf("id: 'project'") < settingsFeature.indexOf("id: 'import'"), 'project settings must appear between interface and import settings');
assert(settingsFeature.includes("activeSection === 'project'") && settingsFeature.includes('新建项目时创建策划文件夹') && settingsFeature.includes('projectCategories.map') && settingsFeature.includes('removeProjectCategory'), 'project settings must own project creation and custom category management');
assert(app.includes('const customProjectCategories = normalizeProjectCategories') && app.includes('projectCategoryOrder: normalizeProjectCategoryOrder') && systemIpc.includes('normalizeCustomProjectCategories(config?.customProjectCategories)') && systemIpc.includes('normalizeProjectCategoryOrder(config?.projectCategoryOrder'), 'custom project categories and their order must be normalized in both renderer migration and main-process persistence');
assert(projectNavigator.includes('normalizeProjectCategoryOrder(projectCategoryOrder, customProjectCategories)') && projectWorkspace.includes('normalizeProjectCategoryOrder(projectCategoryOrder, customProjectCategories)') && workspaceIpc.includes('availableProjectStatuses(catalog).map'), 'custom, ordered, and preserved project categories must be available in every project status menu');
assert(settingsFeature.includes('draggable onDragStart=') && settingsFeature.includes('moveProjectCategory(name, -1)') && settingsFeature.includes('addingProjectCategory') && settingsFeature.includes('<LockKeyhole'), 'project categories must use the sortable list editor while built-in categories remain visibly locked');
assert(workspaceDb.includes('def valid_project_status') && workspaceDb.includes('if not valid_project_status(payload["status"])'), 'the workspace database must accept validated custom project statuses');
assert(systemIpc.includes('生日日期无效') && systemIpc.includes('normalized[name] = `${month}.${day}`'), 'birthday IPC must independently validate and normalize saved dates');
assert(projectWorkspace.includes("if (event.key === 'Enter') { event.preventDefault(); void commitInlineRename(); }") && projectNavigator.includes('<form autoComplete="off" onSubmit='), 'rename editors must submit with Enter without leaking the key to the file row');
assert(projectWorkspace.includes('const breadcrumbs = pathSegments.map') && projectWorkspace.includes('title: props.project.name'), 'project and inspiration breadcrumbs must not repeat their root labels');
assert((projectWorkspace.match(/if \(!changed\) return current;/g) || []).length >= 2, 'file metadata refreshes must preserve state identity when returned details are unchanged');
assert(projectWorkspace.includes("onClick={() => navigateToDirectory('')}") && projectWorkspace.includes('allowSubmenus ? \'overflow-visible\''), 'breadcrumb roots must navigate home and nested surface menus must not be clipped into scrollbars');
assert(projectWorkspace.includes('void loadShellNewTypes();') && projectWorkspace.includes('createShellNewFile(type, target)') && projectWorkspace.includes('Windows 文件类型'), 'surface create submenu must include Windows ShellNew file types for its target folder');
assert(projectWorkspace.includes('const ToolModal') && projectWorkspace.includes("panel === 'negative-import'") && projectWorkspace.includes('从 SD 卡导入</button>') && projectWorkspace.includes('openManualImport'), 'project tools and classified original-material import must use the modal import workflow');
assert(projectWorkspace.includes('chooseImportSourceFiles') && projectWorkspace.includes('chooseWorkspaceDirectory') && projectWorkspace.includes('directSource'), 'negative import must accept explicit files and folders');
assert(toolViews.includes('已选择 ${selectedDrives.length} 个来源，点击右侧按钮开始导入') && toolViews.indexOf('else if (directSource)') < toolViews.indexOf('张卡，点击右侧按钮批量导入'), 'direct negative import must describe files and folders as sources instead of SD cards');
assert(toolViews.includes("if (directSource && status === 'idle') return <ImportSourceControls") && projectWorkspace.includes('onImportComplete={() => { void completeNegativeImport(); }}'), 'negative import must show shared source controls only while idle and retain its independent completion behavior');
assert(types.includes('autoMoveProjectAfterSdImport: boolean') && app.includes('autoMoveProjectAfterSdImport: true') && app.includes('fileConfig.smartImport?.autoMoveProjectAfterSdImport ?? true') && systemIpc.includes('normalizeSdImportAutoMove(config?.smartImport?.autoMoveProjectAfterSdImport)'), 'the SD post-import setting must default on for old configs and persist explicit false values');
assert(settingsFeature.includes('autoMoveProjectAfterSdImport') && /activeSection === 'project'[\s\S]*?title="项目状态"[\s\S]*?导入后自动移动项目分类/.test(settingsFeature), 'project settings must own the SD post-import project-category action');
assert(settingsFeature.includes('启动时自动从 SD 卡导入') && settingsFeature.includes('无人值守导入始终保留卡内源文件') && app.includes('autoStart: false'), 'startup SD import must be clearly named, preserve sources, and default off');
assert(app.includes('setStartupSdAutoStart(normalizedConfig.smartImport.autoStart === true)') && app.includes('useStartupSdAutoImport({ enabledAtLaunch: startupSdAutoStart, enabledNow:') && startupSdAutoImportHook.includes('issuedRef.current = true') && startupSdAutoImportHook.includes('expiresAt: createdAt + STARTUP_SD_IMPORT_WINDOW_MS') && startupSdAutoImportHook.includes("state: 'cancelled'"), 'startup SD import must use the launch snapshot, expire after a bounded window, and allow the live setting to cancel a pending request');
assert(!toolViews.includes('autoCheckStartedRef') && !toolViews.includes('const checkSD') && toolViews.includes('startupAutoImportReady') && toolViews.includes("startBatchRef.current(startupAutoImportSelections, 'startup')") && startupSdAutoImportModel.includes("selectionCount === 0) return 'wait-for-device'") && startupSdAutoImportModel.includes("return 'expired'"), 'startup SD import must wait for ready inventories without surviving beyond its startup window');
assert(toolViews.includes('handledStartupAutoImportRef.current = handledStartupRequestAfterBatchStart(') && startupSdAutoImportModel.includes("mode === 'manual'") && startupSdAutoImportModel.includes('request?.state === \'pending\''), 'a successful manual SD batch must consume the pending startup request before a later inventory update can start it again');
assert(preload.includes("ipcRenderer.invoke('getStorageDevices')") && systemIpc.includes("ipcMain.handle('getStorageDevices'") && storageDeviceService.includes('VolumeSerialNumber') && storageDeviceService.includes('identityStable') && storageDeviceService.includes('eligibleForSdImport') && storageDeviceService.includes('complete: false') && types.includes('sdDevices: ConfiguredSdDevice[]'), 'SD cards must use identity-keyed configuration and report incomplete enumeration instead of trusting a reused drive letter');
assert(toolViews.includes('useStorageDeviceInventory') && !toolViews.includes('setInterval(fetchDrives') && storageDeviceInventoryModel.includes('scheduledHandle = schedule') && storageDeviceInventoryModel.includes('if (running'), 'storage inventory polling must be shared, serialized, and retired with its final subscriber');
assert(storageDeviceService.includes('if ($driveType -ne 2) { continue }') && storageDeviceService.includes("readPlistBoolean(output, 'RemovableMedia')") && storageDeviceService.includes('MEDIA_ROOT_PROBE_TIMEOUT_MS') && storageDeviceService.includes("collectProcessOutput('/bin/test'") && storageDeviceService.includes('summarizeDarwinStorageDeviceResults') && !storageDeviceService.includes('fs.promises.stat(candidate)') && !storageDeviceService.includes('.filter(device => fs.existsSync(device.mountPath))') && !storageDeviceService.includes('internal === false') && toolViews.includes('shouldPollStorageDeviceInventory({') && toolViews.includes('onOpenChange={setImportPanelOpen}'), 'storage polling must filter Windows drives before probing, bound media-root I/O in child processes, tolerate unrelated macOS volume failures, reject merely external disks, and pause behind a collapsed panel after startup');
const windowsEnumerationSource = storageDeviceService.slice(storageDeviceService.indexOf('const WINDOWS_ENUMERATION_COMMAND'), storageDeviceService.indexOf('const windowsDeviceProbeCommand'));
assert(!windowsEnumerationSource.includes('IsReady') && !windowsEnumerationSource.includes('Test-Path') && storageDeviceService.includes('Promise.allSettled([') && storageDeviceService.includes('canonicalIdentity') && storageDeviceService.includes('aliases') && storageDeviceService.includes('deviceErrors') && storageDeviceService.includes('listWindowsStorageDevices = async ({ collect'), 'Windows removable drives must be enumerated quickly, probed independently, and use serial identities with GUID migration aliases');
assert(types.includes('aliases?: string[]') && sdStartupImportModel.includes('storageDeviceMatchesId') && sdStartupImportModel.includes('deviceId: match.id'), 'configured SD cards must match legacy aliases and rewrite them to the canonical device ID');
assert(storageDeviceInventoryModel.includes('warning: string | null') && storageDeviceInventoryModel.includes('deviceErrors:') && toolViews.includes('storageInventory.warning') && toolViews.includes('故障位置'), 'partial storage probe failures must remain visible without downgrading a healthy inventory');
assert(toolViews.includes('isFreshStorageDeviceInventory') && storageDeviceInventoryModel.includes("snapshot.status === 'ready'") && storageDeviceInventoryModel.includes('lastSuccessAt'), 'startup import must require a fresh successful device inventory instead of using stale devices after an enumeration error');
assert(toolViews.includes('scheduleRetry') && toolViews.includes('setWorkspaceProjectsError') && toolViews.includes('startupAutoImportError={storageInventory.error || workspaceProjectsError}'), 'startup SD import must surface and retry device/project readiness failures instead of waiting silently forever');
assert(preload.includes("ipcRenderer.invoke('workspace-finalize-sd-imports'") && workspaceIpc.includes("ipcMain.handle('workspace-finalize-sd-imports'") && types.includes('finalizeSdImportedProjects:') && !preload.includes('workspace-archive-imports') && !types.includes('archiveImportedProjects'), 'SD import finalization must use the semantic API and retire the archive-named bridge');
assert(projectWorkspace.includes('const completeSdImport = async (completion: ImportCompletion)') && projectWorkspace.includes('const completeNegativeImport = async ()') && projectWorkspace.includes('moveProjectAfterImport: importConfig.autoMoveProjectAfterSdImport'), 'SD and direct-source imports must use separate completion handlers');
assert(toolViews.includes('appendImportSuccess(importCompletion()') && importCompletionModel.includes("sourceType: 'work' | 'broll'") && importCompletionModel.includes('event.skipped !== true') && importCompletionModel.includes('count > 0'), 'ImportCard must derive movable work projects only from successful positive-count classifier events');
assert(projectWorkspace.includes('dismissPanelTask(taskKey)') && !projectWorkspace.includes("state: 'completed', progress: 100, message: task?.message || '处理完成'"), 'completed import panels must leave the background task center instead of retaining a duplicate completed state');
assert(toolViews.includes("setStatus('completed')") && toolViews.includes("status === 'completed'") && toolViews.includes('onCompletedAction') && projectWorkspace.includes('panelImportResult?.kind') && projectWorkspace.includes('progressImportCompletion'), 'all import panels must retain a visible completion state until the user closes or refreshes the panel');
assert(toolViews.includes('const orderedProjects = [...(workspaceProjects || [])].sort') && toolViews.includes('orderedProjects.map(project => <option'), 'suggested project routes must appear before the remaining projects in each classification selector');
assert(systemIpc.includes("ipcMain.handle('choose-import-source-files'") && preload.includes("ipcRenderer.invoke('choose-import-source-files')"), 'negative file selection must be exposed through a registered IPC channel');
assert(read('python/classify.py').includes('def scan_direct_media') && read('python/classify.py').includes('args.direct_source') && read('python/classify.py').includes('source_paths =') && toolViews.includes("'--source_paths', JSON.stringify(selectedDrives)"), 'negative import must scan all selected sources as one batch instead of requiring DCIM/PRIVATE');
assert(classifyImport.includes('def stage_media_to_safety_temp') && classifyImport.includes("'stagingComplete': True") && classifyImport.indexOf('write_import_graph_receipt(temp_dir') < classifyImport.indexOf('导入流程全部完成", 100') && workspaceIpc.includes("ipcMain.handle('workspace-media-workflow-import-recover'") && preload.includes("ipcRenderer.invoke('workspace-media-workflow-import-recover'"), 'work imports must persist an authoritative receipt before success and expose main-process recovery');
assert(toolViews.includes('shouldDeleteSourceAfterImport') && toolViews.includes("...(deleteSource ? ['--delete_source'] : [])") && startupSdAutoImportModel.includes("mode === 'manual'") && projectWorkspace.includes('deleteSourceAfterImport: true') && projectWorkspace.includes('deleteSourceAfterImport: draft.deleteSourceAfterImport') && classifyImport.includes('if delete_source:') && workspaceIpc.includes('options?.deleteSourceAfterImport === true') && brollImport.includes('options?.deleteSourceAfterImport === true'), 'manual imports must retain their explicit source-deletion choice while startup imports force source preservation');
assert(toolViews.includes('setShouldDeleteSourceAfterImport(deleteSourceAfterImport)') && projectWorkspace.includes("active={active && panel === 'import'}") && projectWorkspace.includes("active={active && panel === 'negative-import'}"), 'SD-card and negative-import panels must refresh their source-deletion choice from settings whenever opened');
assert(!projectNavigator.includes('从 SD 卡导入</button>') && !projectNavigator.includes('从文件名选片</button>'), 'project-list context menus must not duplicate project import tools');
const inspirationTools = read('python/inspiration_tools.py');
const pythonBuild = read('scripts/build-python.cjs');
const appDialogProvider = read('src/components/AppDialogProvider.tsx');
const indexCss = read('src/index.css');
assert(app.includes("titlebarTabDragProps(projectTabId(page.id))") && app.includes("titlebarTabDragProps(workspaceToolTabId(tab.ownerPageId, tab.kind))") && app.includes("titlebarTabDragProps('settings')") && titlebarTabOrder.includes("window.addEventListener('pointermove', move") && titlebarTabOrder.includes('setOrder(current =>') && titlebarTabOrder.includes("window.localStorage.setItem(TITLEBAR_TAB_ORDER_KEY"), 'visible title-bar tabs must support pointer reordering and persist their order');
assert(app.includes('ownerPageId: string; projectId: string') && app.includes('tab.ownerPageId === page.id') && app.includes('activePageId === tab.ownerPageId') && titlebarTabOrder.includes('tab.ownerPageId === page.id'), 'project tool tabs must be owned and activated by a specific page instance');
assert(app.includes('const disposePageOwnedUi') && app.includes('dismissPanelTasksByOwnerPageId(pageId)') && app.includes('current.filter(tab => !pageIdSet.has(tab.ownerPageId))') && app.includes('disposePageOwnedUi(closingPageIds)'), 'every page close path must dispose its page-owned panel tasks and tool tabs through one lifecycle');
assert(app.includes('tab.ownerPageId !== ownerPageId || tab.kind !== kind || tab.busy === busy'), 'tool busy state must not leak between two pages of the same project');
assert(app.includes('item.id === ownerPageId') && projectWorkspace.includes('isPanelTaskRestoreForPage(pageId, detail)') && !app.includes('item.project?.path === scopeKey'), 'panel task restoration must activate and open only its exact owner page');
assert(app.includes('pages: projectPages, activePageId') && !app.includes('const [openProjects') && app.includes('activePageId === page.id') && app.includes('key={page.id}'), 'project workspaces and active titlebar state must use page instance identity instead of project path uniqueness');
assert(app.includes('pageId={page.id}') && app.includes('onDirectoryChange={updatePagePath}') && projectWorkspace.includes('onDirectoryChangeRef.current?.(currentRelativePath)') && projectWorkspace.includes('onDirectoryChange?.(pageId, relativePath)'), 'each mounted project workspace must report directory changes to its own page instance through a stable callback');
assert(projectWorkspace.includes('selectedRelativePaths={selectedPaths}') && projectVersionTree.includes('selectedNodeIds') && projectVersionTree.includes('rgb(148 163 184 / 0.025)'), 'version tree multi-selection must feed group dragging and keep the canvas dot grid visually subdued');
assert(app.includes('updateProject(nextProject)') && app.includes('closeAllPagesForProject(project)') && workspaceTabModel.includes('page.projectId === project.id') && workspaceTabModel.includes('page.projectId !== projectId'), 'project mutations must update or close every page sharing the database project id');
assert(app.includes('const nextPage = remaining[0]') && app.includes('browserPageActivation(nextPage)') && workspaceTabModel.includes("page?.kind === 'inspiration'"), 'deleting the active project must follow the model-selected remaining page and show inspiration before falling back to home');
assert(titlebarTabOrder.includes('project-page:${pageId}') && titlebarTabOrder.includes("id.startsWith('project:')") && titlebarTabOrder.includes('legacyPath'), 'titlebar ordering must use page ids and migrate matching legacy path-based project ids');
assert(app.includes("page.initialRelativePath ? `${project.name} · ${folderName || page.initialRelativePath}` : project.name"), 'root project pages must use the project name while child pages include their current folder name');
assert(app.includes('openProjectDirectoryPage') && app.includes('currentRelativePath: initialRelativePath, initialRelativePath') && projectWorkspace.includes('onOpenDirectoryPage(entry.relativePath)'), 'opening a project folder in a new tab must create a distinct mounted workspace at that initial path');
assert(projectWorkspace.includes('if (!active)') && projectWorkspace.includes('filterRatingSequenceRef.current') && projectWorkspace.includes('if (active && !wasActiveRef.current)'), 'inactive project pages must suspend rating scans and resume directory work only when activated');
assert(marqueeSelectionModel.includes('calculateFileGridGeometry') && marqueeSelectionModel.includes('fileSurfaceContentWidth') && projectWorkspace.includes('FILE_SURFACE_HORIZONTAL_PADDING') && !projectWorkspace.includes('surface.clientWidth - 48'), 'file virtualization and marquee selection must derive grid geometry from one padded content width');
assert(indexCss.includes('::-webkit-scrollbar { width:12px; height:12px; background:transparent; }') && indexCss.includes('::-webkit-scrollbar-track, ::-webkit-scrollbar-corner { background:transparent; }') && indexCss.includes('border:2px solid transparent;') && indexCss.includes('background-clip:padding-box;') && !indexCss.includes('scrollbar-edge-active') && !app.includes('detectNearbyScrollbar'), 'all visible scrollbars must share one fixed-width, borderless, surface-aware style');
assert(!indexCss.includes('.terminal-scroll::-webkit-scrollbar') && !indexCss.includes('.scrollbar-thin::-webkit-scrollbar') && !indexCss.includes('.project-navigator-scroll::-webkit-scrollbar'), 'individual pages and utility classes must not override the unified scrollbar appearance');
assert(indexCss.includes('.project-menu-item { display: flex;') && indexCss.includes('align-items: center; gap: .5rem;'), 'all dropdown and context menu items must place labels to the right of their icons');
const shellNewService = read('electron/services/shell-new-service.cjs');
const protectedProjectFolderService = read('electron/services/protected-project-folder.cjs');
const mediaAccessService = read('electron/services/media-access-service.cjs');
const mediaResponseService = read('electron/services/media-response-service.cjs');
const telemetryService = read('electron/services/telemetry-service.cjs');
assert(!mediaResponseService.includes('Readable.toWeb(fs.createReadStream') && mediaResponseService.includes('source?.destroy()'), 'media protocol streams must own cancellation and ignore late file events');
assert(telemetryService.includes('if (!app.isPackaged) return false') && telemetryService.includes('recentCrashFingerprints'), 'crash telemetry must exclude development runs and deduplicate repeated failures');
assert(main.includes('createWindow(false);') && main.indexOf('registerAdvancedVideoIpc({') < main.indexOf('loadMainWindowRenderer();', main.indexOf('registerAdvancedVideoIpc({')), 'all IPC handlers must be registered before the renderer is loaded');
assert(!app.includes('getWorkspaceProjects(') && app.includes('loadStartupSnapshot') && systemIpc.includes("ipcMain.handle('load-startup-snapshot'"), 'startup must hydrate config and birthdays together without blocking configuration on a project scan');
assert(workspaceCatalogClient.includes('const inFlight = new Map') && workspaceCatalogClient.includes('workspace-catalog-snapshots:v1') && projectNavigator.includes('getWorkspaceCatalog') && toolViews.includes('getWorkspaceCatalog'), 'project consumers must share one stale-while-revalidate catalog client and persisted startup snapshot');
assert(projectNavigator.includes('refreshQueuedFreshRef.current ||= fresh') && projectNavigator.includes('refreshQueuedCachedOnlyRef.current &&= cachedOnly'), 'queued catalog refreshes must preserve the strongest fresh-versus-cached request semantics');
assert(workspaceIpc.includes('workspaceCatalogs.get(root) || await refreshWorkspaceCatalog(root)') && workspaceService.includes('reconcileRepository.syncCatalog(root)') && main.includes('reconcileRepository: workspaceMaintenanceRepository'), 'interactive project reads must return cached catalogs while filesystem reconciliation runs on the maintenance worker');
assert(!workspaceRepository.includes('operationsRepository?.load(root)') && workspaceRepository.includes("client.call(root, 'init')"), 'read-only project catalog initialization must not cold-start the operations database');
assert(mediaIpc.includes('thumbnailService.evictCache({') && main.includes('await thumbnailService.evictCache({')
  && thumbnailDb.includes('def detach_cache_batch') && thumbnailDb.includes('def prune_missing_batch')
  && thumbnailDb.includes('def recover_cache_publications') && thumbnailDb.includes('thumbnails_cache_access')
  && !mediaIpc.includes('fs.promises.unlink(filePath)'), 'manual, dated, capacity, missing, and orphan cache eviction must use the indexed batch protocol');
assert(thumbnailDb.includes('CREATE TABLE IF NOT EXISTS thumbnail_publish_receipts')
  && thumbnailDb.includes('def resolve_thumbnail_publish') && thumbnailDb.includes('publish_id')
  && thumbnailPipeline.includes('resolveThumbnailPublish(publishId, publishRequest)')
  && thumbnailPipeline.includes("receipt?.state === 'NOT_FOUND' && publishRequest")
  && !thumbnailPipeline.includes("outcome?.state === 'NOT_FOUND'")
  && thumbnailPipeline.includes("outcome?.state === 'COMMITTED'")
  && thumbnailPipeline.includes("error?.code === 'EPOCH_STALE'")
  && thumbnailPipeline.includes("error.publishOutcome = 'unknown'"),
'thumbnail publication commits must be token-idempotent and preserve finals while an ambiguous outcome is resolved');
assert(thumbnailDb.includes('def begin_cache_maintenance')
  && !thumbnailDb.includes('UPDATE thumbnails SET cache_epoch=?')
  && !thumbnailDb.includes('thumbnails.cache_epoch=cache_control.cache_epoch'),
'maintenance epochs must fence only uncommitted workers without rewriting or invalidating committed thumbnail rows');
const nativeRecycleBinService = read('electron/native/RecycleBinService.cs');
assert(recycleBinService.includes("['probe-many']") && nativeRecycleBinService.includes('ProbeMany(ReadValues(5000))'), 'deleted-project maintenance must batch recycle-bin probes into one native helper process');
assert(systemIpc.includes('componentStatusCache.integrityTokens') && systemIpc.includes('seedIntegrityToken') && systemIpc.includes('lastDetailedAttemptAt') && systemIpc.includes('policy.shouldProbeRuntime') && systemIpc.includes('forceRuntimeProbe') && systemIpc.includes('componentStatusForceQueued ||= force'), 'component status refresh must reuse integrity metadata while runtime probes honor TTL, failure backoff, and queued manual refresh');
const packageJson = JSON.parse(read('package.json'));
assert(main.includes("'thumbnail_image', 'video_preview', 'workspace_db'") && pythonBuild.includes("'--hidden-import', 'thumbnail_image'") && pythonBuild.includes("'--hidden-import', 'video_preview', '--hidden-import', 'workspace_db'") && !pythonBuild.includes("'--name', 'thumbnail-image-worker'") && !pythonBuild.includes("'--name', 'workspace-db-worker'") && !JSON.stringify(packageJson.build.win.extraResources).includes('thumbnail-image-worker') && !JSON.stringify(packageJson.build.win.extraResources).includes('workspace-db-worker'), 'lightweight Python services must share the tools runtime instead of duplicating PyInstaller runtimes');
assert(pythonBuild.includes("const sharedWorkerName = 'PhotoFlowImportWorker'") && main.includes("'python', 'PhotoFlowImportWorker', `PhotoFlowImportWorker${exeSuffix}`") && JSON.stringify(packageJson.build.win.extraResources).includes('artifacts/python/PhotoFlowImportWorker') && JSON.stringify(packageJson.build.mac.extraResources).includes('artifacts/python/PhotoFlowImportWorker') && !JSON.stringify(packageJson.build).includes('artifacts/python/tools'), 'the shared Python worker must use its explicit PhotoFlowImportWorker executable and packaged resource name');
assert(['react', 'react-dom', 'lucide-react'].every(dependency => !packageJson.dependencies[dependency] && packageJson.devDependencies[dependency]), 'renderer-only dependencies must remain build-time dependencies instead of being duplicated in app.asar');
const sharedToolsBuild = pythonBuild.slice(0, pythonBuild.indexOf('const inspirationTools'));
const inspirationToolsBuild = pythonBuild.slice(pythonBuild.indexOf('const inspirationTools'));
assert(!sharedToolsBuild.includes("'--exclude-module', 'PIL._webp'") && inspirationToolsBuild.includes("'--exclude-module', 'PIL._webp'") && inspirationToolsBuild.includes("'--exclude-module', 'PIL._avif'"), 'the shared image runtime must retain WebP while the OpenCV visual-tools runtime excludes unused Pillow format plugins');
assert(main.includes("'.heic', '.heif', '.hif', '.avif'") && main.includes("const IMAGE_PREVIEW_CONVERSION_EXTENSIONS") && mediaIpc.includes('convertedImagePreviewPath(sourcePath, stat, cacheConfig)'), 'HEIC/HEIF images must be indexed and converted to a browser-compatible original preview');
assert(!PLUGIN_DEFINITIONS['raw-decoder-libraw'] && !componentBuilder.includes("'raw-decoder-libraw':") && pythonBuild.includes("'--collect-binaries', 'rawpy'") && pythonBuild.includes("'--hidden-import', 'raw_decoder', '--hidden-import', 'rawpy'") && !pythonBuild.includes("'--exclude-module', 'rawpy'") && pythonBuild.includes("entry.name.startsWith('PhotoFlow-raw-decoder-libraw-')"), 'LibRaw/rawpy must ship in the main Python runtime and stale optional-component artifacts must be retired');
assert(imageThumbnailRuntime.includes('runImageDecoderWithRawFallback(originalPreviewPool') && imageThumbnailRuntime.includes("runPythonJsonAction('raw_decoder.py'") && !main.includes('RAW_DECODER_COMPONENT_ID') && rawDecoder.includes('use_camera_wb=True') && rawDecoder.includes('output_color=rawpy.ColorSpace.sRGB') && rawDecoder.includes('user_flip=0'), 'RAW thumbnails and original previews must fall back from embedded JPEG extraction to the isolated built-in camera-white-balance sRGB decoder');
assert(main.includes("kind === 'raw' ? RAW_DECODER_CACHE_VERSION : 'builtin'") && rawDecoder.includes('exif[274] = orientation'), 'RAW preview caches must include the decoder version and preserve source orientation');
assert(/\btsc\s+-b\b/.test(packageJson.scripts.build), 'production build must type-check referenced TypeScript projects');
assert(app.includes("videoPreviewQuality: 'medium'") && app.includes('normalizeVideoPreviewQuality(fileConfig.smartImport?.videoPreviewQuality)'), 'video preview quality must default and migrate to medium');
assert(toolViews.includes('<option value="high">高质量</option>') && toolViews.includes('<option value="balanced">平衡</option>') && toolViews.includes('<option value="small">更小文件</option>'), 'video transcode panel must expose its shared import quality settings');
assert(settingsFeature.includes('<option value="all">全部素材</option>') && settingsFeature.includes('<option value="today">仅今天拍摄的素材</option>') && settingsFeature.includes('<option value="today_yesterday">今天和昨天拍摄的素材</option>'), 'SD import settings must expose all, today, and today-plus-yesterday date ranges');
assert(app.includes('config.usagePreferencesVersion < USAGE_PREFERENCES_VERSION') && app.includes('applyAfterSave: true') && app.includes('usagePreferencesVersion: config?.usagePreferencesVersion || USAGE_PREFERENCES_VERSION'), 'the versioned usage-preferences gate must block all old and new profiles until a successful save, while settings reset preserves completion');
assert(usagePreferencesOnboarding.includes('useState<OpenMode | null>(null)') && usagePreferencesOnboarding.includes('useState<SdHabit | null>(null)') && usagePreferencesOnboarding.includes('useState<RatingHabit | null>(null)') && usagePreferencesOnboarding.includes("sdHabit === 'clear-after-shoot' || limitedDateFilter"), 'usage onboarding must require explicit answers to all three habits and a limited date range when the card retains many shoots');
assert(usagePreferencesOnboarding.includes('itemOpenMode: openMode') && usagePreferencesOnboarding.includes('favoriteDisplayMode: ratingHabit') && usagePreferencesOnboarding.includes('deleteSourceAfterImport: !keepMany') && usagePreferencesOnboarding.includes("dateFilter: keepMany ? limitedDateFilter || 'today_yesterday' : 'all'"), 'usage onboarding choices must map to the existing open, rating, SD date-range, and source-deletion settings');
assert(appChrome.includes('export const WindowControls') && appChrome.includes('export const StartupWindowFrame') && app.includes('<StartupWindowFrame><PrivacyConsentPage') && app.includes('<StartupWindowFrame><UsagePreferencesOnboarding') && appErrorBoundary.includes('<StartupWindowFrame>'), 'window controls must remain available during startup, consent, onboarding, normal use, and renderer error recovery');
assert(usagePreferencesOnboarding.includes('className="brand-logo usage-onboarding__logo"') && !usagePreferencesOnboarding.includes('<span className="usage-onboarding__logo"') && indexCss.includes('place-items:center;'), 'usage onboarding must center its panel and show the logo without a framed container');
assert(app.includes('normalizeWorkspacePaths(fileConfig.workspacePath, fileConfig.workspacePaths)') && app.includes('workspacePaths={config.workspacePaths}') && app.includes('workspacePath={project.workspacePath || config.workspacePath}') && systemIpc.includes("...(Array.isArray(config?.workspacePaths) ? config.workspacePaths : [])"), 'legacy workspacePath config must migrate to normalized multiple roots while each opened project keeps using its owning root');
assert(workspaceIpc.includes('id: project.id') && workspaceTabModel.includes('projectId: string') && workspaceTabModel.includes('id: string'), 'workspace projects and browser pages must keep stable project identity separate from page instance identity');
assert(workspaceTabModel.includes('createBrowserPage') && workspaceTabModel.includes('activateBrowserPage') && workspaceTabModel.includes('updateBrowserPagePath') && workspaceTabModel.includes('closeBrowserPage') && workspaceTabModel.includes('updateProjectPages') && workspaceTabModel.includes('closeProjectPages'), 'workspace tab behavior must live in a pure model before titlebar migration');
assert(workspaceTabModel.includes("page.kind === 'project' && page.projectId === project.id && page.currentRelativePath === ''") && workspaceTabModel.includes('selectProjectFromSidebar') && workspaceTabsHook.includes('selectSidebarProject'), 'sidebar project selection must reuse a project page currently at the root or create one without collapsing child pages');
assert(workspaceTabModel.includes("page.kind === 'inspiration' && page.inspirationRootPath === rootPath && page.initialRelativePath === ''"), 'a pinned inspiration root tab must retain its identity while navigating inside the library');
assert(app.includes('if (replacePath !== undefined)') && app.includes('onSelectProject={(project, replacePath) => selectSidebarProjectPage(project, replacePath)}') && projectNavigator.includes('onSelectProject({ ...result.project, workspacePath: projectWorkspacePath }, renameProject.path)') && projectNavigator.includes('else if (result.project) onSelectProject('), 'project metadata updates must update existing page data without using the sidebar-open behavior');
assert(projectWorkspaceLifecycle.includes("kind: 'refresh'") && projectWorkspaceLifecycle.includes('relativePath: currentRelativePath') && projectWorkspaceLifecycle.includes('resetNavigation: false') && projectWorkspace.includes('projectLifecycleRef'), 'same-project path, name, and status changes must preserve each page current directory and navigation state');
assert(settingsFeature.includes('const WorkspaceFoldersPicker') && settingsFeature.includes('添加工作目录') && settingsFeature.includes('设为默认') && settingsFeature.includes('默认写入目录'), 'storage settings must add, remove, and choose the default among multiple project roots');
assert(projectNavigator.includes('Promise.all(requestedWorkspacePaths.map') && projectNavigator.includes('workspacePath: resolvedRoot') && projectNavigator.includes("project?.workspacePath || workspacePath") && projectNavigator.includes('configuredWorkspacePaths.length < 2'), 'the project homepage must merge every configured root, route project actions to the owning root, and periodically refresh multi-root reads');
assert(storageUsageService.includes('...(Array.isArray(config.workspacePaths) ? config.workspacePaths : [])') && storageUsageService.includes("add('workspace', workspaceRoot)"), 'storage accounting must include every configured project root');
assert(toolViews.includes("!directSource && ['plan', 'import', 'broll'].includes(stage)") && toolViews.includes("['--date_filter', config.dateFilter]"), 'date filtering must only be forwarded for real SD-card import stages, not direct negative imports');
assert(classifyImport.includes("if normalized_date_filter != 'all' and original_files:") && classifyImport.includes('filter_media_by_capture_date(') && classifyImport.includes("entry['captureTimestamp'] = capture_timestamp"), 'enabled SD date filtering must run before staging and cache capture times, while all-media imports skip the metadata pre-scan');
assert(/title="导入行为"[\s\S]*?title="RAW 缺 JPG 时自动创建"[\s\S]*?title="导入后默认删除源文件"[\s\S]*?title="导入工作文件"[\s\S]*?title="导入工作文件时执行视频切割"[\s\S]*?title="导入工作文件时执行视频转码"[\s\S]*?title="导入花絮"[\s\S]*?title="导入花絮时执行视频切割"[\s\S]*?title="导入花絮时执行视频转码"/.test(settingsFeature), 'import settings must expose separate work-file and B-roll action groups');
assert(app.includes('generateJpgFromRaw: true') && app.includes('generateJpgFromRaw: fileConfig.importDefaults?.generateJpgFromRaw ?? true'), 'RAW companion JPG generation must default on for new and unmigrated settings');
assert(!settingsFeature.includes('title="导入底片"') && !settingsFeature.includes('title="花絮导入"'), 'legacy negative and B-roll post-processing groups must be removed');
assert(/activeSection === 'project'[\s\S]*?title="新建项目"[\s\S]*?title="新建项目时创建策划文件夹"[\s\S]*?title="项目分类"/.test(settingsFeature), 'project planning-folder creation and category management must live in the project settings page');
assert(/label: '界面'[\s\S]*?label: '项目'[\s\S]*?label: '导入'[\s\S]*?label: '存储'[\s\S]*?label: '组件管理'/.test(settingsFeature) && !settingsFeature.includes("id: 'inspiration-library'"), 'the primary settings navigation must place project settings between interface and import without an empty inspiration-library page');
assert(settingsFeature.includes("id: 'general', label: '界面', description: '配色、标签与首页', icon: <Palette"), 'interface settings navigation must use its interface-specific label, description, and icon');
assert(settingsFeature.includes("id: 'backup', label: '存储', description: '工作目录、归档与工作区备份', icon: <HardDrive") && !settingsFeature.includes("id: 'storage', label: '缓存'"), 'cache settings must be merged into the storage navigation page');
assert(settingsFeature.includes('const SettingsPageGroup') && settingsFeature.includes('const SettingsRow') && settingsFeature.includes('settings-group-card') && /activeSection === 'general'[\s\S]*?title="外观"[\s\S]*?title="界面配色"[\s\S]*?title="显示角色生日"[\s\S]*?title="文件浏览"/.test(settingsFeature), 'settings pages must use unified grouped cards with one horizontal row per setting');
assert(settingsFeature.includes('title="视频剪辑"') && settingsFeature.includes('title="裁剪导出方式"') && settingsFeature.includes('<option value="fast">快速导出（默认）</option>') && settingsFeature.includes('<option value="exact">精确导出</option>') && app.includes("trim: { exportMode: 'fast' }"), 'video trim export mode must be configurable in settings and default to fast stream copy');
assert(/title="统计"[\s\S]*?title="工作目录"[\s\S]*?title="项目工作目录"[\s\S]*?title="灵感库目录"[\s\S]*?title="项目归档盘位置"[\s\S]*?title="备份"[\s\S]*?title="启用工作区备份"[\s\S]*?title="缓存"[\s\S]*?title="最大缩略图缓存容量"/.test(settingsFeature), 'storage settings must be divided into statistics, directories, backup, and cache groups');
assert(toolViews.includes("args.push('--transcode_import_videos', '--transcode_settings', JSON.stringify(transcodeSettings))"), 'SD-card imports must forward the selected video transcode settings');
assert(toolViews.includes("const queue = directSource") && toolViews.includes("args.push('--split_import_videos')") && toolViews.includes("args.push('--transcode_import_videos', '--transcode_settings', JSON.stringify(transcodeSettings))"), 'work and B-roll imports must share video split and transcode panel settings');
assert(classifyImport.includes('args.split_import_videos, args.transcode_import_videos') && classifyImport.includes('def split_broll_video(input_path, keep_original=False)'), 'SD-card B-roll imports must use the shared split and transcode settings');
assert(classifyImport.includes('def promote_staged_file') && classifyImport.includes('def _move_file_no_replace') && classifyImport.includes('MoveFileExW') && classifyImport.includes('os.link(source, destination)') && classifyImport.includes('allow_atomic_move=True') && classifyImport.includes('temporary_path=None') && ffmpegTranscode.includes('f".photoflow-split-') && classifyImport.includes('split_broll_video(destination, keep_original=True)'), 'B-roll imports must atomically promote same-volume staged files without replacing foreign targets and transactionally split them without a second full copy');
assert(classifyImport.includes('sourceVolumeIdentity') && classifyImport.includes('sourceFingerprint') && classifyImport.includes('source_files_are_safe_to_delete') && toolViews.includes('photoflow:sd-import-sessions:v1'), 'resumable imports must persist sessions, identify the original card, and refuse unsafe source deletion');
assert(classifyImport.includes('reserve_unique_destination') && classifyImport.includes("'pendingDestination': destination") && classifyImport.includes('checkpoint_staged_import(staged_import)') && classifyImport.includes('"phase": "organizing"') && !classifyImport.includes("update_staged_entry(staged_import, entry['staged'], {'pendingDestination': destination})"), 'work imports must durably checkpoint a collision-free batch plan, report per-file organization, and avoid full-manifest writes per file');
assert(classifyImport.includes('if direct_project:') && classifyImport.includes('files_with_time = [(file_path, None) for file_path in temp_files_list]'), 'fixed-project imports must skip unnecessary capture-time extraction');
assert(classifyImport.includes('source_paths=imported_paths_by_target.get(target_folder, [])') && classifyImport.includes('def ensure_import_disk_space'), 'video post-processing must target only newly imported paths and imports must preflight disk space');
assert(classifyImport.includes('def save_staged_capture_times') && classifyImport.includes('staged_files_with_capture_times(staged_import)') && !classifyImport.includes('build_capture_groups(import_files)') && !classifyImport.includes('time.sleep(0.002)'), 'confirmed imports must reuse capture times extracted during planning and must not artificially throttle source copies');
assert(!classifyImport.includes('--backup_path') && !classifyImport.includes('shutil.copytree(target_folder, backup_dst)'), 'retired whole-project import backup copies must not remain reachable');
assert(systemIpc.includes('importStage: classifyStage') && app.includes("importStage === 'plan'") && (app.match(/runBackup\(config\.workspacePath, 'after-import'\)/g) || []).length === 1, 'only completed import execution tasks may trigger the single centralized post-import backup');
assert(backupService.includes('previousByInput') && backupService.includes('canReuse') && backupService.includes('incremental: { reusedFiles, reusedBytes'), 'post-import backups must reuse unchanged content objects instead of retransferring the full workspace');
assert(backupService.includes('withWorkspaceRecoveryLease') && backupService.includes('workspaceSqliteCoordinator.run({')
  && backupService.includes("mode: 'exclusive'") && backupService.includes('runRecoveryPythonAction')
  && backupService.includes('recoveryLeaseContext.getStore()') && main.includes('workspaceSqliteCoordinator, prepareDomainRecovery')
  && ['workspaceDatabase', 'operationsDatabase', 'workspaceMaintenanceDatabase', 'mediaDatabase', 'mediaInteractionDatabase', 'teamRetouchDatabase', 'mediaScanDatabase', 'trackingScanDatabase'].every(name => main.includes(name)),
'database restore/reset tools must run under one five-store exclusive lease after every related Python client is suspended');
assert(backupDb.includes('.restore-project-') && backupDb.includes('PRAGMA quick_check') && backupDb.includes('os.replace(staged_target, destination)')
  && domainRecovery.includes('.restore-project-') && domainRecovery.includes('staged_status = verify(staged)') && domainRecovery.includes('os.replace(staged, destination_path)')
  && teamRetouchStorage.includes('.restore-project-') && teamRetouchStorage.includes('PRAGMA quick_check') && teamRetouchStorage.includes('os.replace(staged, destination_path)')
  && teamRetouchDb.includes('restore_domain_workspace(args.source, args.destination'),
'core, domain, and team restore paths must validate staging databases before atomic replacement');
assert(workspaceIpc.includes('progressImportConflictCache') && workspaceIpc.includes('conflictCacheKey') && workspaceIpc.includes('cachedConflicts'), 'progress-import conflict decisions must reuse recent full-hash comparisons');
assert(classifyImport.includes("destination = unique_broll_destination(broll_folder, os.path.basename(source), will_split)") && !classifyImport.includes("date_folder = os.path.join(broll_folder, date_name)"), 'SD-card and project B-roll imports must both place files directly in the project B-roll folder');
assert(classifyImport.includes('def _image_capture_timestamp(file_path)') && classifyImport.includes('def _video_capture_timestamp(file_path)') && classifyImport.includes('Prefer the media capture time; use filesystem mtime only as a fallback'), 'SD-card project routing must prefer embedded image and video capture timestamps over filesystem modification time');
assert(main.includes('exiftool, exiftoolPath') && systemIpc.includes("runtimeArgs.push('--exiftool_path'") && !systemIpc.includes("--metadata_cache"), 'import workers must receive bundled ExifTool without a persistent capture-time database');
assert(classifyImport.includes('def capture_times_for_files') && classifyImport.includes("'-json', '-G1', '-n', '-fast2' if fast else '-fast'") && classifyImport.includes('CAPTURE_TIME_MEMORY_CACHE') && !classifyImport.includes('sqlite3') && classifyImport.includes('timed_files = capture_times_for_files(files)'), 'capture-time routing must batch fast ExifTool reads, retry uncommon metadata accurately, and cache only within the current task');
assert(main.includes('cleanupRetiredCaptureTimeCache') && retiredCacheService.includes("'capture-time-cache.sqlite3'") && retiredCacheService.includes("`${cachePath}-wal`") && retiredCacheService.includes("`${cachePath}-shm`"), 'startup must remove the retired persistent capture-time database and its SQLite sidecars');
assert(ffmpegTranscode.includes('GPU_VIDEO_ENCODERS = ("h264_nvenc", "h264_qsv", "h264_amf", "h264_mf")') && ffmpegTranscode.includes('*(["-hwaccel", "auto"] if hardware_decode else [])') && ffmpegTranscode.includes('decode_attempts = (True, False) if encoder != SOFTWARE_VIDEO_ENCODER else (False,)'), 'video previews must prefer GPU decode and encode with safe software fallback');
assert(classifyImport.includes('from ffmpeg_transcode import (') && !classifyImport.includes('from ffmpeg_utils import get_ffmpeg_exe') && !classifyImport.includes('build_video_preview_command') && !classifyImport.includes('subprocess.Popen('), 'classify.py must orchestrate imports through the reusable FFmpeg transcoding module instead of owning FFmpeg execution details');
assert(ffmpegTranscode.includes('build_general_transcode_command') && ffmpegTranscode.includes('H265_GPU_VIDEO_ENCODERS = ("hevc_nvenc", "hevc_qsv", "hevc_amf", "hevc_mf")') && ffmpegTranscode.includes('SOFTWARE_H265_VIDEO_ENCODER = "libx265"') && ffmpegTranscode.includes('encoder_candidates = general_transcode_encoder_candidates(ffmpeg_exe, video_mode)') && ffmpegTranscode.includes('GPU 编码器 {encoder} 不可用') && ffmpegTranscode.includes('.photoflow-transcode') && ffmpegTranscode.includes('os.replace(temporary, destination)') && toolViews.includes('<option value="h265">H.265 · 节省空间</option>'), 'general video transcoding must support H.264/H.265, prefer GPU encoders, safely fall back to CPU, and commit validated temporary outputs atomically');
assert(ffmpegTranscode.includes('正在编码（{active_backend}）：{name}{item_count}') && toolViews.includes('statusMessage={sourcesLoading'), 'video transcoding progress must prioritize the active backend, file name, and batch count over encoder diagnostics');
assert(workspaceIpc.includes('folderPaths') && projectWorkspace.includes('initialSourceFolders={videoTranscodeSourceFolders}') && toolViews.includes("'--source-folder', folder") && ffmpegTranscode.includes('def _create_unique_transcode_folder') && ffmpegTranscode.includes('文件夹转码任务不能替换原视频'), 'folder-based video transcoding must preserve source-folder identity and write into a new sibling output folder');
assert(toolViews.includes('已选择 {paths.length} 个视频') && toolViews.includes('批量粘贴路径') && toolViews.includes('removeVideoPath(item.path)') && toolViews.includes("label: '编码中'") && toolViews.includes("label: '失败'"), 'video transcoding inputs must use a grouped queue with removable items and per-file task states while keeping raw path entry available');
assert(projectWorkspace.includes("const VideoTrimTimeline") && projectWorkspace.includes("onChange={changeVideoTrimEdge}") && projectWorkspace.includes('另存为新视频') && projectWorkspace.includes('替换原视频') && !projectWorkspace.includes('fixed inset-0 z-[540]') && workspaceIpc.includes("request.saveMode === 'replace'") && workspaceIpc.includes('replaceVideoFileWithRollback') && workspaceIpc.includes('.photoflow-trim'), 'video trimming must use an in-preview dual-handle timeline and offer save-as-new or rollback-safe source replacement');
assert(cutVideo.includes('def trim_video_fast') && cutVideo.includes('"-c", "copy"') && cutVideo.includes('def trim_video_exactly') && cutVideo.includes('"-c:v", "libx264"') && cutVideo.includes('"-crf", "16"'), 'video trimming must provide fast stream-copy and exact re-encode modes');
assert(/for cleanup_index, source in enumerate\(original_files\):\s+ensure_not_cancelled\(\)/.test(classifyImport) && classifyImport.includes('deleted_source_count += 1') && classifyImport.includes('已停止继续清理源文件'), 'SD-card B-roll source cleanup must stop between files when cancellation is requested and report partial cleanup accurately');
assert(/if \(isClipboardSelection\) \{\s*setClipboardPending\(true\);\s*\}[\s\S]*?result = await projectWorkspaceClient\.projectFileOperation/.test(projectWorkspace), 'copy and cut must remain pending until Windows clipboard synchronization succeeds');
assert(/if \(operation === 'copy' \|\| operation === 'cut'\) \{[\s\S]*?clipboardOperationSequenceRef\.current !== clipboardOperationSequence[\s\S]*?setCutPaths\([\s\S]*?setClipboardHasFiles\(true\)[\s\S]*?setClipboardPending\(false\)[\s\S]*?onNotice\(/.test(projectWorkspace), 'copy and cut success state must only be confirmed by the latest completed clipboard write');
assert(/if \(!result\.success\) \{[\s\S]*?clipboardOperationSequenceRef\.current === clipboardOperationSequence[\s\S]*?setCutPaths\(previousCutPaths\)[\s\S]*?setClipboardHasFiles\(previousClipboardHasFiles\)[\s\S]*?setClipboardPending\(false\)/.test(projectWorkspace), 'failed clipboard synchronization must restore the previous confirmed clipboard state');
assert(projectWorkspace.includes('pasteClipboardGeneration') && projectWorkspace.includes('pasteCutPathsSnapshot') && projectWorkspace.includes('result.consumedCutClipboard') && !projectWorkspace.includes("if (operation === 'paste') setCutPaths([])"), 'old paste completions must not clear a newer renderer clipboard generation');
assert(projectWorkspace.includes("event.key === 'Escape' && cutPaths.length") && projectWorkspace.includes('void cancelFileCut()') && projectWorkspace.includes('clipboardOperationSequenceRef.current !== cancellationSequence') && projectWorkspace.indexOf("event.key === 'Escape' && cutPaths.length") < projectWorkspace.indexOf("event.key === 'Escape' && selectedPaths.length"), 'Escape must safely cancel an active cut before clearing the file selection');
assert(preload.includes("ipcRenderer.invoke('workspace-cancel-file-cut'") && filesIpc.includes("ipcMain.handle('workspace-cancel-file-cut'") && main.includes('const cancelSystemFileCut = async expectedSources'), 'cut cancellation must clear only the matching queued system file cut');
assert(projectWorkspace.includes("update.state !== 'STALE'") && projectWorkspace.includes('forgetMediaThumbnailPreviews(update.filePath)') && projectWorkspace.includes('updatedAt: update.sourceMtimeMs ?? Date.now()'), 'external media changes must invalidate renderer thumbnails and publish the new source revision');
assert(projectWorkspace.includes("state === 'STALE'") && projectWorkspace.includes('setSourceRevision(version => version + 1)') && projectWorkspace.includes('entry?.updatedAt, cacheConfig.directory'), 'visible thumbnails and the preview pane must reload when an external source changes');
assert(projectWorkspace.includes('folder.mediaKind === mediaKind && !folder.folderMissing') && projectWorkspace.includes('const hasVersionProgressForEntry'), 'version management must recognize every available progress folder');
assert(/openVersions[\s\S]*?if \(!hasVersionProgressForEntry\(target\)\)/.test(projectWorkspace), 'version management must require a registered progress folder without requiring automatic tracking');
assert(projectWorkspace.includes('progressTrackingAction(fileMenuRegisteredProgressFolder)') && projectWorkspace.includes('刷新版本跟踪') && projectWorkspace.includes('void refreshProgressTracking(progressFolder)'), 'registered main nodes must expose the unified V2 refresh action');
assert(!projectWorkspace.includes('ensureSelectionBaseline')
  && !projectWorkspace.includes('fileMenuIsSelectionBaselineFolder')
  && projectWorkspace.includes('executeManualSelection')
  && !preload.includes('ensureSelectionBaseline')
  && !preload.includes('workspace-selection-baseline-ensure')
  && !versionsIpc.includes('workspace-selection-baseline-ensure')
  && !types.includes('ensureSelectionBaseline'), 'selection V2 must be the only selection entry point; the retired automatic V0 baseline API must not remain exposed or registered');
assert(selectionService.includes('sourceFolderPageSize: 500')
  && selectionService.includes('maxDirectoryDepth: 32')
  && selectionService.includes('maxDirectoriesPerTask: 10000')
  && selectionService.includes('maxSourceFiles: 50000')
  && selectionService.includes('folderListingCursors')
  && selectionIpc.includes("event?.sender?.send?.('workspace-selection-progress'")
  && preload.includes("ipcRenderer.on('workspace-selection-progress'")
  && types.includes('nextCursor?: string | null'), 'selection folder and media scans must use bounded pagination, cancellation progress, and an opaque cursor boundary');
assert(versionsIpc.includes("ipcMain.handle('workspace-final-version-export'")
  && versionsIpc.includes("const parentProgressId = String(request.parentProgressId || '')")
  && !versionsIpc.includes('parentProgressId: latestRoot?.id')
  && !versionsIpc.includes('const latestRoot ='), 'favorite export must require an explicit parent ID instead of inferring the latest version');
assert(versionsIpc.includes("current.nodeRole !== 'progress'") && !versionsIpc.includes("current.versionKey === '0'"), 'V2 progress edit permissions must be role-based rather than version-key based');
assert(!projectWorkspace.includes('relationEditMode')
  && !projectVersionTree.includes('editingRelations')
  && !projectVersionTree.includes('onEditingRelationsChange')
  && projectWorkspace.includes('const [draggingChildId, setDraggingChildId]')
  && projectWorkspace.includes('const [hoverParentId, setHoverParentId]')
  && projectWorkspace.includes('const [pendingRelationChange, setPendingRelationChange]')
  && projectWorkspace.includes('const [relationMutationId, setRelationMutationId]'), 'relation mutation state must remain page-local without restoring the retired relation edit mode');
assert(!projectVersionTree.includes('编辑关系')
  && !projectVersionTree.includes('完成关系编辑')
  && projectVersionTree.includes('group-hover/version-node:opacity-100')
  && projectVersionTree.includes('data-relation-parent-id')
  && projectVersionTree.includes('data-edge-child-handle')
  && projectVersionTree.includes("event.key === 'Delete'")
  && projectVersionTree.includes('progressRelationChangeError'), 'version relation editing must use hover/pointer/keyboard controls without restoring the old top toolbar');
assert(projectVersionTree.includes('useVersionTreeCanvas')
  && projectVersionTree.includes('canvas.nodePointerHandlers(item.key)')
  && projectWorkspace.includes('draggable={false}')
  && versionTreeCanvas.includes('Math.hypot(deltaX, deltaY) < DRAG_THRESHOLD')
  && versionTreeCanvas.includes('setPointerCapture(event.pointerId)')
  && versionTreeCanvas.includes("enqueueSave('patch'")
  && versionTreeCanvas.includes("enqueueSave('replace'")
  && versionTreeCanvas.includes('await loadServerLayout(')
  && versionTreeCanvas.includes("event.code === 'Space'"), 'version tree nodes must use thresholded pointer-capture dragging with one-shot persistence, rollback reload, refresh replacement, and free-canvas panning');
assert(versionTreeCanvas.includes('export type VersionTreeDragState')
  && versionTreeCanvas.includes("type: 'node'")
  && versionTreeCanvas.includes("type: 'relation'")
  && versionTreeCanvas.includes("type: 'pan'")
  && projectVersionTree.includes('dragStateRef.current')
  && projectVersionTree.includes('beginRelationDrag')
  && projectVersionTree.includes('group-focus-within/version-node:opacity-100')
  && projectVersionTree.includes('h-5 w-5')
  && projectVersionTree.includes('role="tooltip"')
  && projectWorkspace.includes('progressRelationChangeError(progressFoldersRef.current, childProgressId, parentProgressId)'), 'node, relation, and pan interactions must share an exclusive drag state while hover/focus ports retain explicit client validation feedback');
const restoreVersionTreeLayoutBody = projectWorkspace.match(/const restoreStandardVersionTreeLayout = async \(\) => \{[\s\S]*?\n  \};/)?.[0] || '';
assert(projectWorkspace.includes("kind: 'files' | 'version-tree-layout'")
  && projectWorkspace.includes("surfaceMenu.kind === 'version-tree-layout'")
  && projectWorkspace.includes("!blankCanvas || normalizeProjectRelativePath(currentRelativePath) || !versionTreeModeAvailableFor()")
  && projectWorkspace.includes('恢复版本树标准排版')
  && projectWorkspace.includes('将恢复标准排版。版本关系和文件不会改变。')
  && restoreVersionTreeLayoutBody.includes('controller.refreshLayout()')
  && !restoreVersionTreeLayoutBody.includes('refreshProgressTracking')
  && !restoreVersionTreeLayoutBody.includes('refresh(')
  && !projectVersionTree.includes('aria-label="刷新版本树布局"')
  && /saveVersionTreeLayout[\s\S]*?if \(result\.success\) \{[\s\S]*?if \(applyOnSuccess \|\| mergedAfterConflict\) \{[\s\S]*?applyPositions\(applyOnSuccess \|\| mergedAfterConflict!\)/.test(versionTreeCanvas), 'blank root version-tree context refresh must stay isolated from tracking/files and only publish default coordinates after atomic replace succeeds');
assert(projectWorkspace.includes("projectWorkflows && (!scopePath || hasVersionTreeFor(foldersToCheck, scopePath))")
  && projectWorkspace.includes("remembered === 'version-tree' && !versionTreeModeAvailableFor(foldersToCheck, normalizedPath)")
  && projectWorkspace.includes("return hasVersionTreeFor(foldersToCheck, normalizedPath) ? 'version-tree' : 'grid'")
  && projectWorkspace.includes('const projectVersionTreeAvailable = versionTreeModeAvailableFor()')
  && projectWorkspace.includes('window.sessionStorage.getItem(folderBrowseModeStorageKey)')
  && projectWorkspace.includes('window.sessionStorage.setItem(folderBrowseModeStorageKey'), 'the project root must always expose version-tree mode while automatic entry remains gated by a real visible tree and the current window remembers explicit view choices');
assert(projectWorkspace.includes('const folderGridIconSizeStorageKey = `photoflow:folder-grid-icon-sizes:')
  && projectWorkspace.includes('window.sessionStorage.getItem(folderGridIconSizeStorageKey)')
  && projectWorkspace.includes('window.sessionStorage.setItem(folderGridIconSizeStorageKey')
  && projectWorkspace.includes('setGridIconSize(gridIconSizeForFolder(lifecycle.relativePath))')
  && projectWorkspace.includes('setGridIconSize(gridIconSizeForFolder(normalizedPath))')
  && projectWorkspace.includes('rememberFolderGridIconSize(currentRelativePathRef.current, current + direction * intensity)')
  && projectWorkspace.includes('onChange={event => selectFolderGridIconSize(Number(event.target.value))}'), 'each folder must restore and persist its own grid icon size for both the slider and Ctrl+wheel');
assert(projectWorkspace.includes('ProgressRelationMutationQueue')
  && projectWorkspace.includes('relationMutatingChildIds')
  && projectWorkspace.includes('expectedUpdatedAt: latestChild.updatedAt')
  && projectWorkspace.includes('const queue = new ProgressRelationMutationQueue()')
  && projectWorkspace.includes('relationMutationQueueRef.current = queue')
  && projectWorkspace.includes('queue.dispose()')
  && !projectWorkspace.includes('relationMutationIdRef.current !== mutationId'), 'relation mutations must serialize per child and must not discard successful responses using a global mutation ID');
assert(versionsIpc.includes("ipcMain.handle('workspace-legacy-selection-relation-repair'")
  && preload.includes('repairLegacySelectionRelation:')
  && types.includes('interface LegacySelectionRelationRepair')
  && !projectWorkspace.includes('LegacySelectionRepairNotice'), 'legacy repair support may remain available to trusted callers but its migration prompt must not be mounted in the version tree');
assert(/TARGET_SCHEMA_VERSION = \d+/.test(workspaceDb)
  && workspaceDb.includes('CREATE TABLE IF NOT EXISTS version_tree_layouts')
  && workspaceDb.includes('CREATE TABLE IF NOT EXISTS version_tree_node_positions')
  && versionsIpc.includes("ipcMain.handle('workspace-version-tree-layout-get'")
  && versionsIpc.includes("ipcMain.handle('workspace-version-tree-layout-save'")
  && preload.includes('getVersionTreeLayout:')
  && preload.includes('saveVersionTreeLayout:')
  && types.includes('interface VersionTreeLayoutResult')
  && projectVersionTree.includes('nodeKey: `progress:${node.id}`')
  && !projectVersionTree.includes("team-workspace:primary"), 'version-tree coordinates must use persisted progress/workflow node keys and a constrained preload/API boundary');
assert(workspaceDb.includes('CREATE TABLE IF NOT EXISTS version_graph_edges')
  && workspaceDb.includes("VERSION_GRAPH_EDGE_KINDS = (\"media_companion\", \"derived_preview\", \"workflow_input\")")
  && workspaceDb.includes("PROGRESS_NODE_ROLES = (\"original\", \"progress\", \"selection\", \"artifact\", \"workflow\")")
  && workspaceDb.includes('artifact_kind TEXT')
  && workspaceDb.includes('def version_graph_edge_create')
  && workspaceDb.includes('def version_graph_edge_delete')
  && workspaceDb.includes('只能提交项目 ID、节点 ID 和关系类型')
  && workspaceDb.includes("source.media_kind=target.media_kind")
  && workspaceDb.includes("target.node_role='progress'")
  && types.includes("'artifact' | 'workflow'")
  && types.includes("artifactKind?: 'companion' | 'preview' | 'team_workspace'"), 'schema 24 must keep structural parents singular while adding constrained, ID-only supplemental graph edges and artifact/workflow roles');
assert(!/ipcMain\.(?:handle|on)\s*\(/.test(main), 'main.cjs must not own IPC handlers');
assert(lines(main) < 2000, 'main.cjs exceeded the architecture size budget');
assert(versionsIpc.includes('registerVersionTrackingIpc') && versionTrackingIpc.includes("ipcMain.handle('workspace-progress-tracking-start'") && versionTrackingIpc.includes("ipcMain.handle('workspace-progress-tracking-commit'"), 'version tracking must remain isolated behind its own IPC registrar');
assert(/workspaceDatabase = new PythonDatabaseClient\([\s\S]*?defaultTimeoutMs: 2 \* 60 \* 1000/.test(main), 'workspace recovery must allow long project-catalog reconciliation to finish');
assert(lines(appEntry) < 950, 'App.tsx exceeded the architecture size budget');
assert(!projectWorkspace.includes('window.electronAPI') && projectWorkspace.includes('projectWorkspaceClient') && projectWorkspaceContract.includes('Pick<IElectronAPI, ProjectWorkspaceApiKey>') && projectWorkspaceClient.includes('ProjectWorkspaceApi'), 'the workspace renderer must compile against its explicit preload contract');
assert(pluginContributions.includes('workspaceSurfaces') && pluginContributions.includes('ipcNamespaces') && pluginContributions.includes('serviceCapabilities') && app.includes('componentIdForSettingsSection'), 'optional plugins must declare UI, IPC, and service contribution metadata');
assert(!/run(?:Workspace|Media)Database/.test(`${main}\n${workspaceIpc}\n${versionsIpc}\n${versionTrackingIpc}`), 'IPC code bypassed repositories');
assert.strictEqual((app.match(/electronAPI\.getComponents\(/g) || []).length, 1, 'App must be the single renderer owner of component status');
assert(!app.includes('shrink-0 font-mono text-[10px] text-slate-400">v'), 'the title-bar brand must not display the application version');
assert(!settingsFeature.includes('electronAPI.getComponents('), 'settings must consume App component state instead of fetching it');
assert(settingsFeature.includes("title: '删除已使用的安装包吗？'") && settingsFeature.includes('删除并释放 ${size}') && settingsFeature.includes("kind: 'component'") && settingsFeature.includes("kind: 'advanced'"), 'component and advanced package installers must offer optional cleanup with the actual size');
assert(!projectWorkspace.includes('electronAPI.getComponents('), 'project workspace must consume App component state instead of fetching it');
assert(!preload.includes('checkScript') && !read('electron/modules/system-ipc.cjs').includes("ipcMain.handle('check-script'") && !fs.existsSync(path.join(root, 'src', 'features', 'plugins', 'RequirePlugin.tsx')), 'bundled Python tools must not retain optional-plugin existence checks or missing-plugin UI');
assert(app.includes("DEFAULT_HOME_ORDER: HomeCardId[] = ['birthday', 'import', 'inspiration']") && app.includes("requestInspirationPath(config.inspirationLibrary.rootPath.trim(), '')") && !app.includes('openResearchTab') && !app.includes('openConverterTab') && !app.includes("setActiveTab('video_transcode')"), 'the home page must request an inspiration root page without standalone storyboard, PNG, or video-transcode tools');
assert(app.includes('整理和浏览灵感素材') && !app.includes('识别视频转场并提取清晰分镜帧') && !app.includes('批量转换图片格式') && !app.includes('<HomePanel title="灵感库"'), 'the home page must keep only the direct inspiration launcher');
assert(!app.includes("installedComponentIds.has('research-tools')"), 'the inspiration library must not be gated by a component install');
assert(projectWorkspace.includes("teamRetouchAvailable && fileMenu.entry.kind === 'image'"), 'team retouch context-menu contribution must require the installed component');
assert(projectWorkspace.includes("const dismissLoadingNotice = onNotice('正在加载团片协作数据…', 30000)") && projectWorkspace.includes("if (typeof dismissLoadingNotice === 'function') dismissLoadingNotice()") && projectWorkspace.includes('teamRetouchOpening || !teamRetouchInstalled && componentsLoading') && projectWorkspace.includes('团片协作已加载，共 ${combined.size} 张图片'), 'opening team retouch must immediately expose loading, completion, and busy-button feedback while dismissing the loading toast');
assert(settingsFeature.includes('filter(item => installedComponentIds.has(item.componentId))'), 'component settings contributions must require the installed component');
assert(app.includes("delete componentSettings['research-tools']") && app.includes('const researchSettings:') && app.includes('const inspirationLibrary:'), 'legacy research component config must migrate into the built-in storyboard panel defaults');
assert(inspirationLibrary.includes('FileBrowserWorkspace') && inspirationLibrary.includes('INSPIRATION_FILE_BROWSER_CONTEXT') && inspirationLibrary.includes('InspirationLibraryNavigator') && browserContext.includes('PROJECT_FILE_BROWSER_CONTEXT') && browserContext.includes('INSPIRATION_FILE_BROWSER_CONTEXT'), 'project and inspiration shells must compose the shared file browser through explicit contexts');
assert(workspaceIpc.includes("ipcMain.handle('workspace-folder-tree'") && inspirationLibrary.includes('listWorkspaceFolders') && inspirationLibrary.includes('visibleFolders.map'), 'the inspiration sidebar must recursively list folders only through the workspace IPC boundary');
assert(!inspirationLibrary.includes('刷新灵感库目录') && !inspirationLibrary.includes('window.setInterval(() => void loadFolders(), 2500)') && workspaceIpc.includes("ipcMain.handle('workspace-watch-file-root'") && fileRootWatcherService.includes('const watchers = new Map()') && fileRootWatcherService.includes('references += 1'), 'the inspiration directory tree must use a ref-counted multi-root watcher instead of normal-operation polling');
assert(app.includes("projectPages.filter(page => page.kind === 'inspiration').map(page =>") && app.includes('<InspirationLibraryPage pageId={page.id}') && app.includes("className={active ? 'h-full w-full' : 'hidden'}"), 'every inspiration page must stay independently mounted like project pages');
assert(!app.includes('inspirationTabOpen') && !app.includes('inspirationRelativePath') && !app.includes('inspirationNavigationRequest'), 'inspiration pages must not depend on singleton open, path, or navigation-request state');
assert(workspaceTabModel.includes('selectInspirationPath') && workspaceTabModel.includes('replaceInspirationRootPages') && app.includes('resetInspirationPages(rootPath, config.pinInspirationLibrary)'), 'inspiration path requests must reuse pages while root changes remove stale-root pages safely');
assert(titlebarTabOrder.includes('inspiration-page:${pageId}') && app.includes('inspirationTabId(page.id)') && app.includes("page.currentRelativePath ? `灵感库 · ${folderName || page.currentRelativePath}` : '灵感库'"), 'inspiration titles and titlebar ordering must use page instance ids');
assert((projectWorkspace.match(/inspirationMode/g) || []).length <= 5 && projectWorkspace.includes('projectWorkflows') && projectWorkspace.includes('gatherToProject') && projectWorkspace.includes('rootRelativeFileEvents'), 'file-browser feature gates must use explicit capabilities instead of scattered page-mode branches');
assert(!projectWorkspace.includes('导入底片</button>') && !projectWorkspace.includes('导入进度</button>') && !projectWorkspace.includes('导入花絮</button>') && !projectWorkspace.includes('导入文件</button>') && (projectWorkspace.match(/openManualImport\(projectWorkflows \? 'original' : 'files'/g) || []).length === 2, 'manual import menus must collapse the material-specific entries into one unified import action');
assert((projectWorkspace.match(/projectWorkflows && <button className="project-menu-item"[\s\S]{0,250}?setPanel\('import'\)[\s\S]{0,100}?<\/button>/g) || []).length === 2, 'SD-card import must only appear in project file-browser menus, never in the inspiration library');
assert(!inspirationLibrary.includes('onOpenToolTab={() => undefined}') && !inspirationLibrary.includes('onProjectMoved={() => undefined}'), 'the inspiration shell must not satisfy project-only contracts with no-op callbacks');
assert(app.includes("rootPath: ''") && inspirationLibrary.includes('attemptedInitialChoiceRef') && inspirationLibrary.includes('chooseWorkspaceDirectory(rootPath)'), 'first use of the inspiration library must request its root folder instead of silently defaulting to Downloads');
assert(settingsFeature.includes('updateInspirationLibraryRoot') && settingsFeature.includes('选择后立即保存'), 'changing the inspiration-library folder in settings must persist immediately');
assert(!settingsFeature.includes("activeSection === 'research'") && !settingsFeature.includes('默认导出 JPG 画质') && toolViews.includes('initialTargetPath') && toolViews.includes("args.push('--organize-data')"), 'storyboard and PNG tools must be contextual panels without standalone settings');
assert(workspaceIpc.includes("ipcMain.handle('workspace-add-inspiration-to-project'") && workspaceIpc.includes("path.join(targetRoot, '策划')") && workspaceIpc.includes('shell.writeShortcutLink') && workspaceIpc.includes('await copyFileAtomic(source, destination)'), 'inspiration gathering must create folder shortcuts and copy files into the selected project planning folder');
assert(workspaceIpc.includes("ipcMain.handle('workspace-resolve-shortcut'") && workspaceIpc.includes('shell.readShortcutLink') && projectWorkspace.includes('resolveProjectShortcut') && app.includes('onOpenInspirationPath={navigateInspiration}'), 'project shortcuts must resolve safely and navigate into the configured inspiration library');
assert(projectWorkspace.includes('inspiration-target-button') && projectWorkspace.includes('inspirationTargetProject.name') && projectWorkspace.includes('添加到项目') && projectWorkspace.includes('addInspirationToProject'), 'the inspiration browser must expose its selected target project and project gathering actions');
assert(inspirationLibrary.includes('添加到项目') && inspirationLibrary.includes('renamingPath === relativePath') && inspirationLibrary.includes("'rename'") && inspirationLibrary.includes("'trash'"), 'the inspiration sidebar must expose inline rename, project gathering, and recycle-bin deletion for folders');
assert(inspirationLibrary.includes('onOpenInNewTab(relativePath)') || inspirationLibrary.includes('onOpenInNewTab(path)') && inspirationLibrary.includes("application/x-photoflow-folder-tab"), 'the inspiration sidebar must expose explicit new-tab opening and a folder drag payload');
assert(folderTabNavigation.includes("'data-folder-tab-drop-zone': 'true'") && folderTabNavigation.includes('openInNewTab(payload.relativePath)') && folderTabNavigation.includes("payload.kind === 'project'") && app.includes('<div {...folderTabDropProps} data-folder-tab-drop-zone="true" aria-label="标签栏"') && app.includes("openProjectInNewTab: project => openProjectDirectoryPage(project, '')") && !app.includes('拖到顶部栏即可在新标签页打开') && projectNavigator.includes("JSON.stringify({ kind: 'project', project })") && projectWorkspace.includes("[data-folder-tab-drop-zone=\"true\"]") && projectWorkspace.includes("new Event('photoflow:folder-tab-drag-start')") && projectWorkspace.includes('onOpenDirectoryPage(draggedEntry.relativePath)'), 'only the real titlebar tab strip must accept inspiration folders, sidebar projects, and project file-browser folders as new tabs');
assert(app.includes('browserNavigationRequests[page.id]') && folderTabNavigation.includes('setNavigationRequests') && folderTabNavigation.includes('requestInspirationPath(rootPath, relativePath)'), 'ordinary inspiration sidebar navigation must target the active page instead of creating another tab');
assert(inspirationLibrary.includes('photoflow:inspiration-collapsed-paths:') && inspirationLibrary.includes('readInspirationCollapsedPaths(rootPath)') && inspirationLibrary.includes('writeInspirationCollapsedPaths(rootPath, next)'), 'the inspiration sidebar must persist collapsed folder paths separately for each inspiration-library root');
assert(projectWorkspace.includes('截取分镜帧') && projectWorkspace.includes('openResearchForEntries') && projectWorkspace.includes("selectedEntries.every(entry => entry.kind === 'video' || isFolderLikeEntry(entry))") && projectWorkspace.includes("panel === 'research'") && projectWorkspace.includes('researchTargetHasTxt'), 'every file browser must expose contextual storyboard extraction for files, folders, managed external folders, and multi-selection');
assert(!projectWorkspace.includes('selectedToolSourceAvailability') && !projectWorkspace.includes('fileMenuToolSourceAvailability') && projectWorkspace.includes("isFolderLikeEntry(entry) || entry.kind === 'video'") && projectWorkspace.includes("isFolderLikeEntry(entry) || entry.extension.toLocaleLowerCase() === '.png'"), 'video and PNG tool visibility must use the selected entry types without scanning folder contents');
assert(projectWorkspace.includes('fileMenuEntries.map(entry => entry.path)') && projectWorkspace.includes('fileMenuHasToolActions && <div'), 'folder PNG conversion must pass the selected paths to the converter and omit the tool separator when no contextual tools are visible');
assert(!settingsFeature.includes("saving ? '保存中…' : '保存设置'") && settingsFeature.includes("onNotice('已更改设置')"), 'settings must save changes live and report the change through the shared toast');
assert(settingsFeature.includes('pendingSaveRef') && settingsFeature.includes('while (pendingSaveRef.current)'), 'rapid live setting changes must be serialized and coalesced safely');
assert(settingsFeature.includes('恢复默认设置') && settingsFeature.includes('getDefaultSettings') && app.includes('DEFAULT_CONFIG(userPath'), 'general settings must expose restoration from the application default configuration');
assert(projectWorkspace.includes('GalleryVerticalEnd') && projectWorkspace.includes("type ProjectBrowseMode = 'recent' | 'grid' | 'list'") && projectWorkspace.includes('最近文件（包含子文件夹和文件夹快捷方式）') && projectWorkspace.includes('listRecentProjectFiles'), 'the shared file browser must expose recent, grid, and list as mutually exclusive browsing modes');
assert(projectWorkspace.includes("type ProjectFileFilter = 'all' | 'media' | 'image' | 'video'") && projectWorkspace.includes("if (fileFilter === 'image')") && projectWorkspace.includes("if (fileFilter === 'video')") && projectWorkspace.includes("{ value: 'image', label: '图片' }") && projectWorkspace.includes("{ value: 'video', label: '视频' }"), 'the shared file browser must filter all media, images including RAW, or videos independently of the search query');
assert(types.includes("export type ProjectFilterScope = 'current-folder' | 'project-root'") && projectWorkspace.includes("useState<ProjectFilterScope>('current-folder')"), 'filter scope must be page-local and default to direct children of the current folder');
assert(browserContext.includes("rootFilterLabel: '当前项目'") && browserContext.includes("rootFilterLabel: '全部文件'") && projectWorkspace.includes('browserContext.rootFilterLabel'), 'filter scope labels must come from the shared browser context instead of page-mode branches');
assert(projectWorkspace.includes("filterScope === 'project-root'") && projectWorkspace.includes('listProjectFiles(workspacePath, project.status, project.name') && projectWorkspace.includes('parentRelativePath') && !projectWorkspace.includes("searchProjectFiles(workspacePath, project.status, project.name, '', '')"), 'whole-project filtering must use paginated enumeration and grouped parent metadata rather than one unbounded empty search');
assert(projectWorkspace.includes('scopeFileListFilter') && projectWorkspace.includes('scopeCursor, scopeFileListFilter') && projectWorkspace.includes('scopeRequestSequenceRef.current += 1'), 'project-root query and file type changes must start and continue only their own cancellable paginated session');
assert(projectWorkspace.includes('scopeRequestSequenceRef.current += 1') && projectWorkspace.includes('cancelScopeSession()') && projectWorkspace.includes("setSelectedPaths([])") && projectWorkspace.includes('replaceScopeCursor'), 'scope changes and page deactivation must cancel stale enumeration, clear its cursor, and reset selection');
assert(projectWorkspace.includes("converterTriggerAction(panel === 'converter', projectPanelIsRunning('converter'))") && projectWorkspace.includes("triggerAction === 'close'") && projectWorkspace.includes('closePngConverterPanel()'), 'the PNG conversion entry must delegate open, close, and running-task restoration to its lifecycle decision');
assert(projectWorkspace.includes('showVideoToolsMenu') && projectWorkspace.includes('截取分镜帧') && projectWorkspace.includes('openVideoTranscode') && projectWorkspace.includes("mountedPanels.has('video-transcode')") && projectWorkspace.includes('result.videoPaths') && toolViews.includes('sourcesLoading'), 'the single video-tools toolbar icon must open contextual storyboard/transcode panels and automatically populate indexed selected videos');
assert(toolViews.includes('chooseVideoFolder') && toolViews.includes("targetPaths.flatMap(path => ['--path', path])") && toolViews.includes("start(targetPaths, '正在扫描视频...')") && projectWorkspace.includes('initialTargetPaths={videoSplitTargets}'), 'storyboard extraction and video splitting must accept folders, one file, or multiple files');
assert(systemIpc.includes("scriptName === 'cut_video.py'") && cutVideo.includes('parser.add_argument("--cancel_file"') && cutVideo.includes('emit("cancelled"'), 'batch video splitting must remain cancellable through the shared Python task boundary');
assert(projectWorkspace.includes('findCachedMediaThumbnailPreview') && projectWorkspace.includes("result.state === 'QUEUED'") && projectWorkspace.includes('update.previewUrls?.large || update.previewUrls?.medium || update.previewUrls?.small'), 'the media preview pane must reuse visible thumbnails and keep queued preview work in a loading state');
const focusEntrySource = projectWorkspace.slice(projectWorkspace.indexOf('const focusEntry'), projectWorkspace.indexOf('const activateMediaPreview'));
assert(focusEntrySource.indexOf('setSelectedPaths([entry.relativePath])') < focusEntrySource.indexOf('setPreviewPath(entry.relativePath)') && /const activateMediaPreview[\s\S]*?focusEntry\(entry\)/.test(projectWorkspace) && /const openPreviewFromMenu[\s\S]*?focusEntry\(entry\)/.test(projectWorkspace), 'every live media preview entry must share selection-first focus behavior so contextual toolbar actions stay in sync');
assert(projectWorkspace.includes('setMetadataPaneAutoOpenSuppressed(true)') && projectWorkspace.includes('!metadataPaneAutoOpenSuppressed') && projectWorkspace.includes('metadataPaneSuppressedStorageKey') && projectWorkspace.includes('onClose={closeMetadataPaneByUser}'), 'closing image metadata must persistently suppress automatic reopening until the user explicitly reopens or pins it');
assert(/const navigableEntries = previewEntry\.kind === 'video'[\s\S]*?const nextEntry = navigableEntries\[nextIndex\][\s\S]*?setSelectedPaths\(\[nextEntry\.relativePath\]\)[\s\S]*?setPreviewPath\(nextEntry\.relativePath\)/.test(projectWorkspace), 'keyboard preview navigation must keep the active selection synchronized with the displayed media');
assert(projectWorkspace.includes('previewResourcePathRef') && projectWorkspace.includes('current.sourcePath === entry.path') && projectWorkspace.includes('Keep an already') && projectWorkspace.includes('decoded original visible'), 'media detail hydration must not replace an already decoded original with a lower-resolution thumbnail');
assert(projectWorkspace.includes('data-recursive-folder-path') && projectWorkspace.includes('handleRecursiveFolderDrop') && projectWorkspace.includes('internalMovePathsForTarget') && projectWorkspace.includes("runFileOperation('paste', undefined, [], target)"), 'each recursive result group must behave as its own directory for paste and internal drag-to-move operations');
assert(projectWorkspace.includes('data-recursive-folder-readonly') && projectWorkspace.includes('快捷方式指向的外部文件夹是只读浏览区域'), 'recursive folder regions reached through shortcuts must remain read-only');
assert(projectWorkspace.includes('browseProjectShortcutPreview(workspacePath, project.status, project.name, entry.relativePath)') && !/browseProjectFiles\([^\n]*entry\.path/.test(projectWorkspace), 'shortcut covers must use the main-validated preview IPC instead of browsing renderer-supplied external paths');
assert(projectWorkspace.includes('IntersectionObserver') && projectWorkspace.includes("if (entry.shortcutTargetKind || entry.shortcutBroken) return") && projectWorkspace.includes("if (entry.kind === 'folder' || entry.kind === 'shortcut') void loadDirectoryPreviewEntries(entry)"), 'shortcut resolution must be lazy for visible or hovered entries');
assert(projectWorkspace.includes("shortcutPreviewStatesRef") && projectWorkspace.includes("shortcut:${entry.relativePath}:${entry.updatedAt}") && projectWorkspace.includes('directoryEntriesCacheRef.current.set(cacheKey, result.entries)'), 'shortcut cover caches must include source path and modification time while keeping external entries out of active project entries');
assert(workspaceIpc.includes("ipcMain.handle('workspace-recent-files'") && workspaceIpc.includes('recentFileSessions') && workspaceIpc.includes('maximumDirectoriesPerPage = 64') && workspaceIpc.includes('maximumInspectedEntriesPerPage = 4000') && projectWorkspace.includes('loadMoreRecentFiles') && projectWorkspace.includes('RECENT_FILES_LOAD_AHEAD_PX'), 'recent recursive browsing must continue through a bounded cursor session as the user scrolls');
assert(workspaceIpc.includes("ipcMain.handle('workspace-list-files'") && workspaceIpc.includes('fileListSessions') && workspaceIpc.includes('maximumDirectoriesPerPage = 32') && workspaceIpc.includes('maximumInspectedEntriesPerPage = 1000') && workspaceIpc.includes("ipcMain.handle('workspace-cancel-list-files'"), 'plain recursive file enumeration must be paginated, bounded, and cancellable');
assert(workspaceIpc.includes('projectFileListSessionMatches(session, root, scope, filter)') && workspaceIpc.includes('parentRelativePath') && workspaceIpc.includes("fileListSessionExpiredCode = 'FILE_LIST_SESSION_EXPIRED'"), 'file-list cursors must bind their root, scope, and normalized filter signature while returning grouping metadata with stable expiry errors');
assert(mediaRatingIpc.includes("ipcMain.handle('workspace-media-rating-read-batch'") && mediaRatingIpc.includes('Math.min(6, entries.length)') && mediaRatingService.includes('`${path.resolve(filePath)}|${updatedAt}`'), 'batch rating reads must reuse a path-and-updatedAt cache with bounded main-process concurrency');
assert(projectWorkspace.includes('pending.slice(offset, offset + 200)') && projectWorkspace.includes('getMediaRatings') && projectWorkspace.includes("if (!active || ratingFilter === 'all')") && projectWorkspace.includes('filterRatingsCheckedCount'), 'rating filters must inspect only enumerated finite batches and remain dormant when no rating is requested');
assert(workspaceIpc.includes("recentFilesSessionExpiredCode = 'RECENT_FILES_SESSION_EXPIRED'") && projectWorkspace.includes('result.errorCode === RECENT_FILES_SESSION_EXPIRED') && /result = await projectWorkspaceClient\.listRecentProjectFiles\(workspacePath, project\.status, project\.name, currentRelativePath, RECENT_FILES_PAGE_SIZE\);/.test(projectWorkspace), 'expired recent-file cursor sessions must restart automatically without requiring the user to reopen recursive browsing');
assert(mediaAccessService.includes('const rootGrants = new Map()') && mediaAccessService.includes('const grantRoot = value =>') && workspaceIpc.includes('mediaService.grantRoot(mediaRoot)') && (workspaceIpc.match(/mediaService\.grantRoot\(externalScope\?\.mediaRoot \|\| root\)/g) || []).length >= 2, 'validated project and managed-external browse, search, and recent roots must authorize only the active media root');
assert(workspaceIpc.includes("runPythonJsonAction('office_media_extract.py'"), 'Office image extraction must run from the built-in application runtime');
assert(projectWorkspace.includes('提取截图主图') && projectWorkspace.includes("panel === 'screenshot-main-image'") && projectWorkspace.includes('fileMenuScreenshotMainImageEntries'), 'the shared project and inspiration browser must expose screenshot main-image extraction in the toolbar and image context menu');
assert(workspaceIpc.includes("ipcMain.handle('workspace-extract-screenshot-main-images'") && workspaceIpc.includes("runPythonJsonAction('screenshot_main_image.py'") && workspaceIpc.includes("virtualPaths.resolve(projectRoot, relativePath, { externalRootMode: 'target' })"), 'screenshot main-image extraction must validate project-virtual image paths before running');
assert(toolViews.includes('onScreenshotMainImageProgress') && toolViews.includes('<TaskProgress') && toolViews.includes('保留原图'), 'screenshot main-image extraction must use the shared progress card and expose the original-retention option');
assert(toolViews.includes("projectFileOperation(workspacePath, projectStatus, projectName, 'trash', croppedRelativePaths)"), 'successful screenshot crops must recycle their originals by default through the recoverable project-file operation');
assert(toolViews.includes('if (croppedRelativePaths.length) await onFilesChanged?.()') && projectWorkspace.includes('directoryEntriesCacheRef.current.clear()') && projectWorkspace.includes('refreshRecursiveResults(screenshotMainImageTargets.map') && projectWorkspace.includes('await refresh(currentRelativePathRef.current)'), 'screenshot main-image completion must immediately refresh the active file list and affected recursive result groups');
assert(workspaceIpc.includes('thumbnailService.syncChangedPaths(item.root, [item.path]') && workspaceIpc.includes("root: workspaceRoot, fileName: path.relative(workspaceRoot, projectRoot), eventType: 'rename'"), 'screenshot main-image output must queue fresh thumbnails against each validated local or external media root and publish a workspace-root-relative file change');
assert(workspaceIpc.includes("workspace-screenshot-main-image-progress") && main.includes('const runPythonJsonAction = (scriptName, args, timeoutMs = 20 * 60 * 1000, onMessage, signal, deadlineAt)'), 'screenshot main-image extraction must stream per-image progress from the Python JSON protocol');
assert(inspirationTools.includes('"screenshot_main_image": "screenshot_main_image"') && pythonBuild.includes("'--hidden-import', 'screenshot_main_image'"), 'screenshot main-image extraction must be bundled in the core inspiration-tools runtime');
for (const code of ['RECYCLE_BIN_FAILED', 'RECYCLE_UNAVAILABLE', 'RECYCLE_SERVICE_MISSING', 'EPERM', 'EACCES', 'EBUSY']) {
  assert(recycleBinFailure.includes(`'${code}'`), `recycle-bin failure dialog must recognize ${code}`);
}
assert(recycleBinFailure.includes('拒绝访问') && recycleBinFailure.includes('being used by another process'), 'recycle-bin failure dialog must recognize localized Windows denial messages');
assert(recycleBinService.includes("args[0] === 'trash' ? 'RECYCLE_BIN_FAILED'"), 'native trash failures must expose a stable structured error code');
assert(projectWorkspace.includes('isRecycleBinFailure(result.error, result.errorCode)'), 'project and ordinary file deletion must use structured recycle-bin errors');
assert(/missingDirectory && !requestedPath[\s\S]*?onDeleted\(\)/.test(projectWorkspace), 'an externally deleted open project must close its stale tab');
assert(!nativeRecycleBinService.includes('EnsureRecycleCapacity') && !nativeRecycleBinService.includes('CalculateSourceSize'), 'trash must not pre-scan folder size before handing deletion to Windows');
assert(nativeRecycleBinService.includes('FOF_WANTNUKEWARNING') && nativeRecycleBinService.includes('{ "permanent", permanent }'), 'Windows must warn before a non-recyclable item is permanently deleted and report that outcome');
assert(/publish\(\{ phase: 'complete'[\s\S]*?queuePermanentProjectCleanup\(root, projectName\)/.test(workspaceIpc), 'permanent project deletion must release the foreground UI before scheduling internal cleanup');
assert(workspaceIpc.includes("kind: 'project-cleanup'") && workspaceIpc.includes("type: 'deleted-project-cleanup'"), 'permanent project cleanup must be restart-safe and visible as a background task');
assert(workspaceIpc.indexOf('getDeletedProjectCleanupPlan') < workspaceIpc.indexOf('removeInternalProjectArtifacts(root, cleanupPlan)') && workspaceIpc.indexOf('removeInternalProjectArtifacts(root, cleanupPlan)') < workspaceIpc.indexOf('purgeDeletedProject(root, project.id)'), 'deleted project artifacts must be planned and removed before the recoverable database row is purged');
assert(main.includes('isSuppressedWorkspaceChange(root, fileName)') && workspaceIpc.includes('suppressWorkspaceWatchPath(projectPath)') && workspaceIpc.includes('releaseWorkspaceWatchPath(suppressedProjectPath)'), 'project deletion must suppress its recursive workspace watcher event storm');
assert(mediaRatingService.includes('`${item.filePath}_exiftool_tmp`') && mediaRatingService.includes('`${item.filePath}_original`') && mediaRatingService.includes('suppressedPath => suppressWorkspaceWatchPath(suppressedPath)') && mediaRatingService.includes('suppressedPath => releaseWorkspaceWatchPath(suppressedPath)'), 'rating writes must suppress and release ExifTool temporary sibling paths without forwarding Array.forEach indices as release delays');
assert(inspirationLibrary.includes("if (change.eventType === 'change') return;") && inspirationLibrary.includes('pendingTreeScrollTopRef') && inspirationLibrary.includes("scrollIntoView({ block: 'nearest' })"), 'inspiration navigation must ignore content-only writes and preserve the active folder scroll position');
const databaseConnect = workspaceDb.slice(workspaceDb.indexOf('def connect('), workspaceDb.indexOf('def directory_identity('));
assert(databaseConnect.includes('if backup_path or is_fresh:') && !databaseConnect.includes('_automatic_backup_if_due'), 'routine database maintenance must not block opening the workspace catalog');
assert(main.includes('workspaceMaintenanceDatabase = new PythonDatabaseClient') && workspaceIpc.includes("type: 'workspace-database-maintenance'"), 'daily database maintenance must use an independent background worker');
assert(main.includes('workspaceCatalogSemanticSnapshot') && main.includes('stableCatalogValue') && !main.includes('previousSnapshot = JSON.stringify(workspaceCatalogs'), 'workspace reconciliation notifications must compare stable business fields instead of maintenance metadata');
assert(workspaceIpc.includes('workspaceMaintenanceCooldownMs') && workspaceIpc.includes('workspaceMaintenanceScheduledAt.get(root)'), 'project-list reads must cooldown visible workspace maintenance tasks');
assert(projectNavigator.includes('refreshInFlightRef') && projectNavigator.includes('refreshQueuedRef') && projectNavigator.includes('refreshGenerationRef'), 'project catalog refreshes must coalesce concurrent events and ignore stale responses');
assert(filesIpc.includes("new Set(['import', 'move', 'paste', 'trash', 'select', 'rename'])") && filesIpc.includes('suppressWorkspaceWatchPath?.(root)') && filesIpc.includes('releaseWorkspaceWatchPath?.(suppressedProjectRoot)'), 'bulk file mutations must suppress recursive watcher and media-scan storms');
assert(/operation === 'move'[\s\S]*?createProjectFileTask\([\s\S]*?resources: \[destinationDir, \.\.\.sources\][\s\S]*?await task\.start\(\)/.test(filesIpc), 'internal move must use the shared cancellable disk task and path resource locks');
assert(/for \(const \[index, entry\] of movePlan\.entries\(\)\)[\s\S]*?await movePathAtomic\(entry\.source, entry\.destination[\s\S]*?for \(const entry of \[\.\.\.moved\]\.reverse\(\)/.test(filesIpc), 'batch move must use atomic moves and reverse completed items after any failure');
assert(fileTransferService.includes('await collectCopyPlan(resolvedSource, temporary, plan') && fileTransferService.includes('await assertDiskSpace(path.dirname(target), totalBytes)') && fileTransferService.includes('await assertCopyPlanSourcesUnchanged(plan)') && fileTransferService.includes('await removeCopiedSources(plan,'), 'cross-volume directory moves must preserve planned atomic copy, disk-space, source-identity, and safe removal checks');
assert(projectFileTaskService.includes("scanning: '正在统计'") && filesIpc.includes('已发现 ${discoveredCount} 个文件和文件夹'), 'file tasks must immediately expose scanning and periodically report discovered entries');
assert(filesIpc.includes('affectedDirectories') && filesIpc.includes('movedItems') && projectWorkspace.includes('scheduleDirectoryRefresh(result.affectedDirectories'), 'file operation responses must drive targeted renderer directory invalidation');
assert(projectWorkspace.includes('directoryRefreshTimerRef') && projectWorkspace.includes('pendingDirectoryRefreshesRef') && projectWorkspace.includes('}, 180)') && projectWorkspace.includes('scheduleDirectoryRefresh(result.affectedDirectories)') && projectWorkspace.includes('pendingRecursiveDirectories') && projectWorkspace.includes('affectsCurrentDirectory'), 'active operations must aggregate targeted response directories while watcher events debounce only affected visible scopes');
assert(recycleBinService.includes('const trashMany = async filePaths =>') && recycleBinService.includes("['trash-many']") && filesIpc.includes('await recycleBinService.trashMany(existingSources)'), 'multi-selection deletion must use one native recycle-bin process and preserve per-item results');
const componentsListHandler = systemIpc.slice(systemIpc.indexOf("ipcMain.handle('components-list'"), systemIpc.indexOf("ipcMain.handle('components-open-folder'"));
assert(componentsListHandler.includes('pluginService.list()') && componentsListHandler.includes('queueComponentStatusRefresh(force === true)') && !componentsListHandler.includes('listWithSizes'), 'component listing must return cached status before recursive sizing and optionally queue a forced runtime probe');
assert(systemIpc.includes("type: 'component-status-refresh'") && app.includes('onComponentsStatusChanged'), 'detailed component status must refresh through a background event');
assert(systemIpc.includes('queueSystemFilesystemCleanup(cleanupPaths') && versionsIpc.includes('queueCleanupArtifacts(workspaceRoot') && versionsIpc.includes("queueFilesystemCleanup([previousWorkflowDirectory]"), 'committed installs, versions, and workflows must defer obsolete internal-file cleanup');
assert(appDialogProvider.includes('choice: (options: ChoiceDialogOptions)') && appDialogProvider.includes("enqueue('choice', options)"), 'the shared in-app dialog provider must support multi-choice decisions');
assert(filesIpc.includes("kind: 'paste-conflict'") && projectWorkspace.includes("value: 'replace'") && projectWorkspace.includes("value: 'keep-both'"), 'paste conflicts must return a decision request and let the in-app UI replace, keep both, or cancel');
assert(workspaceIpc.includes("kind: 'restore-conflict'") && app.includes("value: 'rename'") && app.includes("value: 'overwrite'"), 'occupied restore targets must be resolved through the in-app choice dialog');
assert(versionsIpc.includes("kind: 'version-fingerprint-mismatch'") && read('src/components/VersionManager.tsx').includes("value: 'relocate'"), 'fingerprint mismatches must be resolved through the in-app choice dialog');
assert(workspaceIpc.includes("kind: 'progress-import-conflict'") && workspaceIpc.includes('sourcePaths: selectedSourcePaths') && projectWorkspace.includes("value: 'skip'") && projectWorkspace.includes('progressConflictPolicy: policy'), 'progress append conflicts must preserve the selected files and resolve through the in-app choice dialog');
assert(!filesIpc.includes('showMessageBox') && !workspaceIpc.includes('showMessageBox') && !versionsIpc.includes('showMessageBox'), 'non-file native message boxes must not remain in workspace file flows');
assert(/requestedItems\.filter\(item => item\.conflict\)[\s\S]*?isDirectory: fs\.statSync\(item\.desiredDestination\)\.isDirectory\(\)/.test(filesIpc), 'paste conflict detection must include same-name files as well as folders');
assert(!filesIpc.includes('if (activeProjectFileOperations.size)') && !workspaceIpc.includes('if (activeProjectFileOperations.size)'), 'file tasks must queue by resources instead of rejecting every concurrent operation');
assert(/operation === 'paste'[\s\S]*?await readSystemFileClipboard\(\)[\s\S]*?createProjectFileTask\([\s\S]*?path: destinationDir, access: 'write'[\s\S]*?uniqueSources\.map\(source => \(\{ path: source, access: clipboardSnapshot\.operation === 'cut' \? 'write' : 'read'[\s\S]*?await task\.start\(\)/.test(filesIpc), 'paste must lock its destination for writing and distinguish copied source reads from cut source writes before filesystem mutation');
assert(/if \(process\.platform === 'win32'\) throw new Error\(`[\s\S]*?Windows[\s\S]*?fileOperationState\.projectFileClipboard/.test(filesIpc), 'Windows clipboard failures must not fall back to stale internal clipboard state');
assert(filesIpc.includes("const destinationResolution = resolveDestination(targetRelativePath)") && filesIpc.includes('const destinationDir = requestedDestination'), 'paste destinations must be checked through the unified virtual-path resolver');
assert(filesIpc.includes('`.photoflow-paste-${operationId}`') && filesIpc.includes('await fs.promises.rename(item.stagedDestination, item.destination)'), 'copied trees must remain hidden until their top-level atomic commit');
assert(filesIpc.includes('`.photoflow-replace-${operationId}`') && filesIpc.indexOf('await stageReplacements();', filesIpc.indexOf('await copyPlannedFiles(plan')) > filesIpc.indexOf('await copyPlannedFiles(plan'), 'replacement targets must be staged only after incoming copies are complete');
assert(filesIpc.includes('samePathIdentity(destination, replacementIdentities.get(pathKey(destination)))'), 'a replacement must reject a target changed after user confirmation');
assert(filesIpc.includes('await removeCopiedSources(plan)'), 'cross-volume cut must validate the source snapshot before removal');
assert(workspaceIpc.includes('isInternalFileOperationEntry(entry.name)'), 'temporary transfer and recovery entries must remain hidden from project browsing');
assert(workspaceIpc.includes("operation.kind === 'paste-replace'"), 'replacement paste operations must have a transactional undo path');
assert(filesIpc.includes("writeLog('info', 'Project files moved by same-volume rename'"), 'same-volume cut/paste must use filesystem moves instead of copy-then-delete');
assert(filesIpc.includes('await movePathAtomic(item.source, item.destination'), 'same-volume cut/paste must use the safe move primitive');
assert(filesIpc.includes('await writeSystemFileClipboard(sources, operation)'), 'copy/cut must wait until the Windows file clipboard is ready');
assert(fileClipboardNative.includes('[STAThread]') && fileClipboardNative.includes('data.SetFileDropList(files)') && fileClipboardNative.includes('Preferred DropEffect') && fileClipboardNative.includes('operation == "cut" ? 2 : 1'), 'the native STA helper must write CF_HDROP and DWORD Preferred DropEffect values');
assert(fileClipboardNative.includes('GetClipboardSequenceNumber') && fileClipboardNative.includes('case "clear-if-current"') && fileClipboardNative.includes('写入后回读验证失败'), 'native clipboard reads, verified writes, and conditional clearing must use Windows sequence numbers');
assert(fileClipboardService.includes('mutationQueue') && fileClipboardService.includes('FILE_CLIPBOARD_SERVICE_MISSING') && !fileClipboardService.includes('powershell.exe') && !main.includes('runWindowsClipboardScript'), 'file clipboard operations must be serialized through the native service without a PowerShell fallback');
assert(filesIpc.includes('clipboardGeneration: clipboardWrite?.sequence'), 'renderer clipboard generations must be confirmed from the native Windows sequence number');
assert((filesIpc.match(/await clearClipboardIfSnapshotCurrent\(clipboardSnapshot\)/g) || []).length === 2 && filesIpc.includes('clearSystemFileClipboardIfCurrent(snapshot)') && !/process\.platform === 'win32'[^\n]*clipboard\.clear\(\)/.test(filesIpc), 'both cut-paste completion paths must delegate sequence-safe clearing to the native helper');
assert(packageJson.scripts.electron.includes('build:file-clipboard-service') && packageJson.scripts['electron:build'].includes('build:file-clipboard-service') && JSON.stringify(packageJson.build.win.extraResources).includes('file-clipboard-service.exe') && fileClipboardBuilder.includes('FileClipboardService.cs'), 'development and packaged Windows builds must compile and ship the native clipboard helper');
assert(filesIpc.includes('missingCutSources') && filesIpc.includes('剪切源已被其他粘贴任务移动或删除'), 'queued paste tasks sharing a cut snapshot must revalidate source existence after acquiring their task lock');
assert(projectNavigator.includes("(showNew || renameProject) && <ProjectDialog title={renameProject ? '重命名项目' : '新建项目'}"), 'project create and rename must share the same editor panel');
assert(projectNavigator.includes('dropProjectOnStatus') && projectNavigator.includes("event.dataTransfer.dropEffect = 'move'") && projectNavigator.includes("event.dataTransfer.effectAllowed = 'copyMove'"), 'sidebar projects must remain draggable to tabs while accepting category-to-category moves');
assert(importSourceControls.includes('onDropPaths') && importSourceControls.includes('getPathForFile(file)') && toolViews.includes('onDropPaths={onDropSourcePaths}') && projectWorkspace.includes('onDropSourcePaths={paths => setNegativeSourcePaths(paths)}'), 'negative import must accept dropped files and folders through the shared source picker');
assert(projectNavigator.includes('createPortal(<div className="fixed inset-x-0 bottom-0 top-10 z-[500]') && projectNavigator.includes('disabled={isCreating || !nextProjectDisplayName}'), 'project editor must escape sidebar stacking and allow a date-only name');
assert(projectNavigator.includes('inspectDroppedExistingProject') && projectNavigator.includes('onDrop={event => void inspectDroppedExistingProject(event)}') && projectNavigator.includes('inspectionToken: existingProjectDraft.inspectionToken'), 'existing-project import must open its panel first, accept a dropped folder, and reuse the validated inspection');
assert(importSourceControls.includes('onDropPaths') && versionProgressPanel.includes('<ImportSourceControls') && versionProgressPanel.includes('onDropPaths={paths => update({ sourcePaths: paths })}') && projectWorkspace.includes('onDropPaths={paths => setBrollSourcePaths(paths)}') && projectWorkspace.includes('onDropPaths={paths => setFileImportSourcePaths(paths)}'), 'negative, progress, b-roll, file, and project import panels must accept dropped files or folders');
assert(versionProgressPanel.includes("draft.mode === 'import' && importStep === 'source'") && versionProgressPanel.includes('下一步：设置进度') && versionProgressPanel.includes('返回重新选择') && projectWorkspace.includes("versionProgressPanelMode === 'import' ? '导入 · 进度'"), 'progress import must separate source selection from version settings');
assert(importSourceControls.includes('title="只导入外链"') && toolViews.includes('onLinkOnlyImport') && projectWorkspace.includes('linkOnly={linkBrollSources}') && projectWorkspace.includes('linkOnly={linkFileSources}') && versionProgressPanel.includes('linkOnly={draft.linkOnly === true}'), 'non-project import panels must keep link-only mode');
assert(importSourceControls.includes("export type ImportMaterialKind = 'original' | 'progress' | 'broll' | 'files'") && importSourceControls.includes('导入的内容') && importSourceControls.includes('原始素材') && importSourceControls.includes('其他文件') && projectWorkspace.includes('onImportKindChange={(kind, sourcePaths) => openManualImport'), 'all manual import panels must share the four-way material-kind selector');
assert(importSourceControls.includes('overflow-hidden rounded-xl border border-slate-200 bg-white') && importSourceControls.includes('mx-4 border-t border-slate-200') && importSourceControls.includes('!rounded-none !border-0'), 'link-only and source-deletion switches must share one compact divided container');
assert(projectNavigator.includes("importExistingProject('move')") && projectNavigator.includes("importExistingProject('copy')") && projectNavigator.includes('剪切并接管') && projectNavigator.includes('复制并接管') && !projectNavigator.includes('importExistingProjectLink') && !projectNavigator.includes('<option value="link">') && !preload.includes('importExistingProjectLink') && !workspaceIpc.includes('workspace-import-existing-project-link'), 'project import must expose only direct copy-and-adopt and move-and-adopt actions');
assert(workspaceIpc.includes("if (!['copy', 'move'].includes(mode))") && workspaceIpc.includes('cachedExistingProjectInspection(options.inspectionToken, sourcePath)'), 'project import must reject forced link mode while reusing the validated inspection');
assert(projectWorkspace.includes('openProjectEntry(focusedEntry, true)') && workspaceIpc.includes("ipcMain.handle('workspace-open-entry'") && workspaceIpc.includes('const error = await shell.openPath(target)'), 'folder details external-open must delegate to the operating-system shell');
assert(workspaceIpc.includes("virtualPaths.resolve(root, normalizedRelativePath, { externalRootMode: 'target' })") && workspaceIpc.includes('virtualPaths.readManagedExternalLink(entryPath)') && projectWorkspace.includes('if (isFolderLikeEntry(entry) && !external)') && projectWorkspace.includes('isFolderLikeEntry(entry) && entry.externalLink && !range') && projectWorkspace.includes("entry.externalLink ? '外链'") && projectWorkspace.includes('entry.externalLink && <span aria-label="外链" className="shortcut-cover-badge"') && projectWorkspace.includes('isFolderLikeEntry(fileMenu.entry)') && projectWorkspace.includes('纳入版本管理…'), 'managed external folders must open in-app on a plain single click, browse through the trusted virtual-path service, use the normal folder layout with an independently overlaid external-link badge, and remain eligible for version management');
assert(workspaceIpc.includes('externalLinkTarget') && workspaceIpc.includes('externalLinkRootRelativePath: currentResolution.shortcutVirtualPath') && projectVersionTree.includes('externalEntryByTarget') && projectVersionTree.includes('isVersionFolderEntry') && projectWorkspace.includes('isFolderLikeEntry') && projectWorkspace.includes('isUnsupportedShortcutContent') && projectWorkspace.includes('currentDirectoryViaExternalLink') && projectWorkspace.includes('aria-label="外链文件夹"'), 'managed external folders must use normal folder presentation, retain an external-link breadcrumb badge, and remain visible as ordinary or registered nodes in the version tree');
assert(workspaceIpc.includes('linkedItems.push({ relativePath:')
  && workspaceIpc.includes('versionService.adoptMediaFolder(importWorkspaceRoot')
  && workspaceIpc.includes('adopted.created === true')
  && workspaceIpc.includes('externalAdoptionUndo: { workspaceRoot: importWorkspaceRoot')
  && workspaceIpc.includes('versionService.revertExternalAdoptions(importWorkspaceRoot')
  && workspaceIpc.includes('externalProgressUndo: { workspaceRoot, projectName, progressId: createdExternalProgressId }')
  && workspaceIpc.includes('operation.externalProgressUndo.workspaceRoot')
  && projectWorkspace.includes('adoptAsOriginal: true')
  && projectWorkspace.includes("await Promise.all([refresh(''), loadProgressFolders()])"), 'link-only original-material imports and external progress imports must atomically register and undo their version-tree nodes');
assert(projectVirtualPathService.includes('externalRootMode') && projectVirtualPathService.includes('allowMissingLeaf') && projectVirtualPathService.includes('外链内容通过重解析点跳出了目标文件夹') && filesIpc.includes('const sourceResolutions = relativePaths.map(resolveSource)') && main.includes("externalRootMode: 'target'"), 'all project consumers must share one sandboxed virtual-path resolver for managed external content');
assert(projectVirtualPathService.includes('MANAGED_EXTERNAL_FILE_PREFIX') && workspaceIpc.includes("externalLinkTargetKind: 'file'") && fileEntrySortModel.includes("entry.externalLinkTargetKind !== 'file'") && workspaceIpc.includes("ipcMain.handle('workspace-relink-external-folder'") && projectWorkspace.includes('重新定位外链'), 'managed external files and folders must both support native app features, offline relinking, and distinct folder navigation semantics');
assert(projectVirtualPathService.includes('linkId: link.linkId') && workspaceIpc.includes('if (resolution.linkId) virtualPaths.revokeManagedExternalLinkIds([resolution.linkId])') && workspaceIpc.includes('if (!relinkCommitted && createdLinkId) virtualPaths.revokeManagedExternalLinkIds([createdLinkId])'), 'external relinking must revoke the replaced identity after commit and the new identity after a failed replacement');
assert(projectVersionTree.includes('inferredExternalOriginalKind') && projectVersionTree.includes("sourceKind: mediaKind") && projectVersionTree.includes('renderEntry(item.entry, item.folder, item.sourceKind)') && projectWorkspace.includes('>原始素材</span>'), 'existing canonical RAW/MOV managed links without historical role metadata must render in the matching original-material area with an original-material badge');
assert(projectNavigator.includes('const legacyMonthDay = project.name.match') && projectNavigator.includes('"9-12-2" becomes date YY-9-12 plus name "2"') && projectNavigator.includes('setEditor(projectEditorValue(project))'), 'legacy M-D and M-D-name projects must reopen with the current year and an optional name');
assert(projectNavigator.includes('type="number" min="0" max="2099"') && projectNavigator.includes('type="number" min="1" max="12"'), 'project date fields must resist Chromium text autofill restoring stale visible values');
assert(app.includes("createPlanningFolder: true") && app.includes("defaultFolderSort: 'date'"), 'project creation and folder sorting defaults must be explicit');
assert(settingsFeature.includes('新建项目时创建“策划”文件夹') && settingsFeature.includes('文件夹默认排序方式'), 'project and interface settings must expose project folder creation and default sorting');
assert(projectNavigator.includes('createWorkspaceProject(workspacePath, projectDate(), name, { createPlanningFolder, workspacePaths: configuredWorkspacePaths })'), 'project creation must forward the planning-folder preference and ordered storage failover roots');
assert(/workspace-create-project'[\s\S]*?selectWorkspaceForWrite\(workspacePath, options\?\.workspacePaths, 0\)/.test(workspaceIpc) && !/workspace-projects'[\s\S]{0,180}options\?\.workspacePaths/.test(workspaceIpc), 'new project creation must select an available configured disk without leaking write-selection logic into project-list reads');
assert(workspaceIpc.includes("ipcMain.handle('workspace-materialize-external-links'") && workspaceIpc.includes('virtualPaths.readManagedExternalLink(shortcutPath)') && workspaceIpc.includes('Unable to roll back external-link materialization') && workspaceIpc.includes('folderPath: relocatedPath'), 'external materialization must accept only identity-verified managed links, relocate version paths, and restore sources and shortcuts on failure');
assert(workspaceIpc.includes("options?.createPlanningFolder !== false") && workspaceIpc.includes("path.join(projectPath, '策划')"), 'workspace creation must default to creating the planning folder while allowing opt-out');
assert(projectWorkspace.includes('useState<ProjectFileSortField>(defaultFolderSort)')
  && projectWorkspace.includes('defaultProjectFileSortDirection(defaultFolderSort)')
  && projectWorkspace.includes('if (field !== sortField) setSortDirection(defaultProjectFileSortDirection(field))')
  && fileEntrySortModel.includes("field === 'name' ? 'asc' : 'desc'"), 'project folders must initialize from the configured sort mode and choose a useful default direction when the sort field changes');
assert(projectWorkspace.includes("const PHOTOSHOP_DOCUMENT_EXTENSIONS = new Set(['.psd', '.psb'])") && projectWorkspace.includes('selectedEntries.every(isPhotoshopOpenEntry)') && projectWorkspace.includes('selectedEntries.filter(isPhotoshopOpenEntry)') && projectWorkspace.includes("if (fileFilter === 'image') return isPhotoshopOpenEntry(entry)"), 'Photoshop actions and image filtering must include images, RAW files, and native PSD/PSB documents');
assert(projectWorkspace.includes('entryPointerModifiersRef.current')
  && projectWorkspace.includes('const additive = event.ctrlKey || event.metaKey || Boolean(pointerModifiers?.additive)')
  && projectWorkspace.includes('fileEntryClickIntent({ openMode: itemOpenMode, selectionCount: selectedPaths.length, range, additive, clickCount })')
  && fileEntryInteractionModel.includes("if (additive || selectionCount > 0) return 'toggle-select'")
  && projectWorkspace.includes("if (intent === 'toggle-select')"), 'Ctrl-click must preserve pointer modifiers and toggle selection instead of opening an entry');
assert(projectWorkspace.includes('directoryEntryToSelectOnReturn(currentRelativePathRef.current, normalizedPath)') && projectWorkspace.includes('pendingDirectoryReturnSelectionRef') && projectWorkspace.includes('setSelectedPaths([returnedFolder.relativePath])') && projectWorkspace.includes('requestFileReveal(returnedFolder.relativePath)'), 'returning to an ancestor directory must select and reveal the child folder that was just left');
assert(types.includes('folderAlphabetFilterEnabled: boolean') && app.includes('folderAlphabetFilterEnabled: true') && app.includes('fileConfig.folderAlphabetFilterEnabled !== false') && settingsFeature.includes('文件夹首字母筛选') && inspirationLibrary.includes('folderAlphabetFilterEnabled={config.folderAlphabetFilterEnabled}') && app.includes('folderAlphabetFilterEnabled={config.folderAlphabetFilterEnabled}'), 'folder alphabet filtering must be enabled by default, configurable in general settings, migrated, and shared by project and inspiration browsers');
assert(folderAlphabetFilterModel.includes('FOLDER_ALPHABET_FILTER_THRESHOLD = 30') && folderAlphabetFilterModel.includes("zh-CN-u-co-pinyin") && projectWorkspace.includes('currentDirectoryFolders.length > FOLDER_ALPHABET_FILTER_THRESHOLD') && projectWorkspace.includes('folderAlphabetKey(entry.name) === folderAlphabetFilter') && projectWorkspace.includes('aria-label="按文件夹首字母筛选"'), 'large grid folders must expose a pinyin-aware A-Z folder filter above the icon grid');
assert(projectWorkspace.includes("active && activeView === 'project' && (viewportStatus || folderOnlyGridCount > 0)") && projectWorkspace.includes(': folderOnlyGridCount}</span>') && projectWorkspace.includes("browseMode === 'grid' && filesInCurrentDirectory.length === 0") && projectWorkspace.includes("if (browseMode !== 'grid' || !viewportCurrentEntry"), 'the active project grid must keep one bottom-right viewport status and show only the folder count when the directory contains no files');
assert(projectWorkspace.includes('data-inline-rename-input="true"') && projectWorkspace.includes("if (pointerTarget.closest('[data-inline-rename-input]')) return;") && (projectWorkspace.match(/onPointerDownCapture=\{handleFileSurfacePointerDownCapture\}/g) || []).length === 2, 'clicking the inline rename input must not let the file-surface capture handler move focus back to the entry');
assert(workspaceIpc.includes('replaceVideoFileWithRollback({ crypto, fs, path, sourcePath, replacementPath: safeGenerated })') && workspaceIpc.includes('.photoflow-trim-output-') && videoTrimCommitService.includes('await renameWithRetry(fs, sourcePath, backupPath') && videoTrimCommitService.includes('await renameWithRetry(fs, backupPath, sourcePath') && projectWorkspace.includes("fallbackVideo.querySelectorAll('source').forEach") && projectWorkspace.includes('const trimEditing = Boolean(trimEditor) && !trimBusy'), 'replacing a trimmed video must release every preview reader and use a rollback-safe Windows file commit');
assert(main.includes("normalized.includes('.photoflow-part')") && workspaceIpc.includes("normalized.includes('.photoflow-part')"), 'workspace watchers and file listings must ignore in-progress media files whose temporary marker appears before the extension');
assert(projectWorkspace.includes('setTrimEditor({ start: 0, end: videoDuration })') && projectWorkspace.includes('previewVideoTrimTime(0)'), 'video trimming must open with the full clip selected even when playback previously reached the end');
assert(cutVideo.includes('parser.add_argument("--trim-mode", choices=("fast", "exact"), default="fast")') && workspaceIpc.includes("'--trim-mode', exportMode") && projectWorkspace.includes('exportMode: videoTools.trim.exportMode') && projectWorkspace.includes('loadVideoTimelineFramesRef.current(times)'), 'video trimming must default to the configured fast mode while retaining exact export and FFmpeg-decoded timeline frames');
assert(cutVideo.includes('"-progress", "pipe:1"') && cutVideo.includes('phase="encoding"') && workspaceIpc.includes("'workspace-video-trim-progress'") && preload.includes('onProjectVideoTrimProgress') && projectWorkspace.includes('role="progressbar"') && projectWorkspace.includes("'取消导出'"), 'video trimming must expose real FFmpeg progress and keep a cancellable inline export status');
assert(projectWorkspace.includes('className="pointer-events-none absolute inset-0 z-40 overflow-hidden bg-slate-600/65') && projectWorkspace.includes('正在裁剪视频…') && projectWorkspace.includes('absolute inset-x-0 top-0 h-1') && !projectWorkspace.includes('className="mt-2.5 rounded-md border border-white/10 bg-white/[.04] px-2.5 py-2"'), 'video trim progress must dim the full timeline and explain progress at its top without adding layout height');
assert(workspaceIpc.includes("type: 'video-trim'") && workspaceIpc.includes('backgroundTasks.create({') && workspaceIpc.includes('taskHandle.context.setCancellable(false)') && projectWorkspace.includes("task.type === 'video-trim'") && projectWorkspace.includes('restoredBackgroundTrimIdRef') && projectWorkspace.includes('sourceDuration: number'), 'video trimming must remain a cancellable main-process background task and restore its progress after the preview pane remounts');
assert(workspaceIpc.includes('startDetachedBackgroundOperation({') && workspaceIpc.includes("result: completedResult") && projectWorkspace.includes('handledVideoTrimTaskIds'), 'video trimming must return after submission and deliver its completed result through the background task model');
assert(workspaceIpc.includes("ipcMain.handle('workspace-cancel-video-trim'") && preload.includes('cancelProjectVideoTrim') && cutVideo.includes('raise SplitCancelled("视频导出已取消")'), 'video trim cancellation must reach the encoder and cleanly stop the export');
assert(projectWorkspace.includes("onNotice('裁剪视频完成')") && projectWorkspace.includes('setPostTrimPreviewEntry(targetPreviewEntry)') && projectWorkspace.includes('setPreviewMediaPath(targetRelativePath)'), 'completed video trims must toast success and automatically preview the exported file, including same-path replacement');
assert(projectWorkspace.includes('initialPaths: [...selectedPaths]') && projectWorkspace.includes('const additive = drag.additive || event.ctrlKey || event.metaKey') && marqueeSelectionModel.includes('initialPaths.filter(path => !hitSet.has(path))') && marqueeSelectionModel.includes('hitPaths.filter(path => !initialSet.has(path))') && projectWorkspace.includes('mergeMarqueeSelection(drag.initialPaths, hits, drag.additive)'), 'Ctrl-drag marquee selection must toggle hit entries relative to the initial selection');
assert(projectWorkspace.includes("from './marquee-selection-model'") && projectWorkspace.includes('mergeMarqueeSelection') && marqueeSelectionModel.includes('viewportPointToContentPoint') && marqueeSelectionModel.includes('normalizeMarqueeRect') && marqueeSelectionModel.includes('gridItemRect') && marqueeSelectionModel.includes('listItemRect') && marqueeSelectionModel.includes('rectanglesIntersect') && marqueeSelectionModel.includes('logicalCanvasSize'), 'marquee geometry and selection merging must live in a DOM-independent pure model');
assert(marqueeSelectionModel.includes('hitMarqueeIndices') && !marqueeSelectionModel.includes('getBoundingClientRect') && !marqueeSelectionModel.includes('document.') && !marqueeSelectionModel.includes('React'), 'marquee hit testing must cover logical unrendered indices without DOM or React dependencies');
assert(projectWorkspace.includes("event.pointerType === 'mouse' && (event.buttons & 1) === 0") && projectWorkspace.includes('event.pointerId !== drag.pointerId') && projectWorkspace.includes('onLostPointerCapture={cancelSelectionDrag}') && projectWorkspace.includes("window.addEventListener('blur', cancelSelectionDrag)"), 'marquee selection must clear stale pointer state after button release, capture loss, or window blur');
const marqueeStartSource = projectWorkspace.slice(projectWorkspace.indexOf('const startSelectionDrag ='), projectWorkspace.indexOf('const updateSelectionDrag ='));
const marqueeFinishSource = projectWorkspace.slice(projectWorkspace.indexOf('const finishSelectionDrag ='), projectWorkspace.indexOf('const cancelFileCut ='));
assert(!marqueeStartSource.includes('setPreviewPaneOpen(false)') && !marqueeStartSource.includes('setMetadataPaneOpen(false)') && marqueeFinishSource.includes('clearPreviewAfterSelectionDrag(drag)') && marqueeStartSource.includes('viewportPointToContentPoint') && marqueeStartSource.includes('startContentY: start.y') && !marqueeStartSource.includes('startContentY: clampNumber'), 'marquee selection must keep the file-column layout and content-space drag origin stable until pointer release');
assert(projectWorkspace.includes('hitMarqueeIndices(selection, displayedFileEntries.length, layout)') && projectWorkspace.includes('finiteLogicalCanvasSize(displayedFileEntries.length, layout') && !projectWorkspace.includes('measuredEntryRects'), 'ordinary grid and list marquee selection must hit every logical entry and size a finite logical canvas instead of relying on rendered DOM nodes');
assert(projectWorkspace.includes('const rebuildMarqueeLayoutRegistry') && projectWorkspace.includes('groupedResultsActive || versionTreeOpen') && projectWorkspace.includes('marqueeLayoutRegistryRef.current = registry'), 'non-formula grouped and version-tree layouts must explicitly register their complete entry geometry');
assert(projectWorkspace.includes('advanceMarqueeAutoScroll(container') && projectWorkspace.includes('if (!result.edgeActive) return') && projectWorkspace.indexOf('window.requestAnimationFrame(runSelectionAutoScroll)', projectWorkspace.indexOf('const runSelectionAutoScroll =')) > projectWorkspace.indexOf('if (result.scrolled)', projectWorkspace.indexOf('const runSelectionAutoScroll =')) && marqueeAutoScroll.includes('scrollHeight - container.clientHeight') && marqueeAutoScroll.includes('scrollWidth - container.clientWidth'), 'marquee selection must retry edge auto-scroll while delayed logical-canvas DOM dimensions are being committed');
assert(projectWorkspace.includes('marquee-logical-canvas') && marqueeSelectionModel.includes('Math.max(content.width, nonNegative(viewport.width)') && marqueeSelectionModel.includes('Math.max(content.height, nonNegative(viewport.height)'), 'the marquee must render on a finite logical canvas at least as large as the viewport, complete content, and current selection');
assert((projectWorkspace.match(/onPointerDown=\{startSelectionDrag\}/g) || []).length === 1 && /ref=\{filesColumnRef\}[^>]*onPointerDown=\{startSelectionDrag\}[^>]*onLostPointerCapture=\{cancelSelectionDrag\}/.test(projectWorkspace) && marqueeStartSource.includes('event.clientY < surfaceRect.top') && !marqueeStartSource.includes('event.clientY > surfaceRect.bottom'), 'marquee selection must start from the full-height file-column side gutters while excluding the toolbar above the file surface');
assert(workspaceIpc.includes("ipcMain.handle('workspace-search-files'") && projectWorkspace.includes('searchResultGroups.map'), 'project search must recurse through its current scope and group results by folder');
assert(projectWorkspace.includes("const currentFolderRecursiveSearchActive = Boolean(searchQuery.trim()) && filterScope === 'current-folder' && !versionTreeOpen && !finalViewOpen") && projectWorkspace.includes('!(recursiveFlatOpen || currentFolderRecursiveSearchActive)') && projectWorkspace.includes('recursiveFlatOpen || currentFolderRecursiveSearchActive ? searchEntries'), 'searching from ordinary grid or list mode must recurse through the current folder instead of filtering only its loaded children');
assert(projectWorkspace.includes("const recursiveScopeLabel = currentRelativePath ? '当前文件夹及其子文件夹' : inspirationMode ? '整个灵感库' : '整个项目'"), 'recursive search scope labels must identify the whole project or inspiration library at root and the current folder below root');
assert(projectWorkspace.includes('纳入版本管理…') && projectWorkspace.includes('isFolderLikeEntry(fileMenu.entry)'), 'any ordinary or managed external project folder must be available as a V2 relationship source');
assert(protectedProjectFolderService.includes("'raw'") && protectedProjectFolderService.includes("'策划'") && protectedProjectFolderService.includes("'团片协作'") && filesIpc.includes('isProtectedProjectFolderPath'), 'remaining workflow-managed root folders must reject ordinary rename operations in both renderer and backend');
assert(projectWorkspace.includes('preflightManualSelection') && projectWorkspace.includes('sourceFolderRelativePath') && !projectWorkspace.includes('entryIsInSelectionSourceFolder(entry)'), 'manual selection must use the current arbitrary source folder through the V2 preflight');
assert(versionsIpc.includes("ipcMain.handle('workspace-progress-update'") && workspaceDb.includes('def progress_update_tree('), 'progress edits must update the complete descendant version tree');
assert(projectWorkspace.includes('const refreshProgressTracking') && projectWorkspace.includes('startProgressTracking') && projectWorkspace.includes("mode: progressFolder.lastTrackedAt || progressFolder.trackingState === 'stale' ? 'refresh' : 'compare'"), 'existing main nodes must refresh through the recoverable V2 background task without treating a never-completed failed compare as tracked');
assert(!projectWorkspace.includes('await Promise.all([loadProgressFolders(), refresh(\'\')]);\n        setSelectedPaths([relativePath]);'), 'modify progress must not wait for a full directory and graph refresh before starting tracking');
assert(!projectWorkspace.includes('inferProgressComparisonParent') && projectWorkspace.includes('folder.id === draft.parentProgressId'), 'V2 parent selection must use parent progress IDs without version-key inference');
assert(versionsIpc.includes('trackingEnabled: progress.id === current.id') && workspaceDb.includes('tracking_enabled=?,tracking_state=?'), 'editing an existing progress must persist its tracking switch');
assert(workspaceDb.includes('missing_since=COALESCE(missing_since,?)') && workspaceDb.includes('path_match["folder_id"] not in present_identities') && projectVersionTree.includes('projectVisibleVersionGraph') && !projectVersionTree.includes('文件夹失效 · 记录已保留'), 'missing progress folders must remain database tombstones while disappearing from the V2 tree projection');
assert(versionsIpc.includes('reconcileExisting: Boolean(request.reconcileExisting)') && workspaceDb.includes('batch is None and payload.get("reconcileExisting")') && workspaceDb.includes('"reconciled": True'), 'existing batch reconciliation must reuse its batch instead of creating duplicate version history');
assert(types.includes('export const PROJECT_TOOLBAR_ACTION_IDS') && settingsFeature.includes('const PROJECT_TOOLBAR_ITEMS: Record<ProjectToolbarActionId') && settingsFeature.includes('value.order.map') && projectWorkspace.includes('projectToolbar.order.filter') && projectWorkspace.includes('hiddenProjectToolbarActions'), 'every project toolbar action must be configured from the shared typed action registry');
assert(app.includes('onlyShowAvailable: source.onlyShowAvailable === true') && settingsFeature.includes('onlyShowAvailable: false'), 'project toolbar availability-only mode must default to off and migrate old settings safely');
assert(settingsFeature.includes('仅显示当前可用的功能图标') && projectWorkspace.includes('!projectToolbar.onlyShowAvailable || projectToolbarAvailability[id]') && projectWorkspace.includes("disabled={!canSelectMedia}"), 'project toolbar must show unavailable actions as disabled by default and hide them only when explicitly configured');
assert(indexCss.includes('@container (max-width: 1040px)') && indexCss.includes('@container (max-width: 760px)') && indexCss.includes('.project-toolbar-overflow-primary { display:contents; }') && indexCss.includes('.project-toolbar-overflow-secondary { display:contents; }') && projectWorkspace.includes('className="project-toolbar-overflow-primary"') && projectWorkspace.includes('className="project-toolbar-overflow-secondary"'), 'project toolbar overflow must collapse file operations first and contextual tools only at the narrower threshold');
assert(projectWorkspace.includes("unavailableProjectToolbarTitle = (label: string, reason: string) => `${label}，${reason}`") && projectWorkspace.includes("unavailableProjectToolbarTitle('PNG 转 JPG'") && projectWorkspace.includes("unavailableProjectToolbarTitle('版本管理'") && !projectWorkspace.includes('当前不可用：${reason}'), 'disabled project toolbar tooltips must contain only the action name and a contextual concise unavailable reason');
assert(projectVersionTree.includes('graph.folders') && !projectVersionTree.includes('onOpenMissingProgressMenu?.(') && workspaceDb.includes('def progress_delete_missing'), 'missing progress-folder nodes must be maintained by the database and never render a legacy invalid-record menu');
assert(projectWorkspace.includes('setSelectedPaths(displayedFileEntries.map(entry => entry.relativePath))'), 'Ctrl+A must select the current displayed folder contents');
assert(shellNewService.includes("Registry::HKEY_CLASSES_ROOT") && shellNewService.includes("Command and handler-based entries are intentionally not executed"), 'ShellNew discovery must be read-only and must not execute registered commands');
assert(!shellNewService.includes('runPowerShellJson(DISCOVERY_SCRIPT).catch(() => [])') && shellNewService.includes('const nextTypes = normalized.slice(0, 80)') && shellNewService.indexOf('cachedTypes = nextTypes') > shellNewService.indexOf('app.getFileIcon(iconSource'), 'ShellNew discovery failures must remain retryable and cache publication must be atomic');
assert(shellNewService.includes("shell-new-types-cache.json") && shellNewService.includes('CACHE_MAX_AGE_MS') && shellNewService.includes('writePersistentCache(cachedTypes)'), 'ShellNew types and icons must persist across app launches instead of rescanning every time');
assert(projectWorkspace.includes('createProjectShellNewFile') && projectWorkspace.includes('Windows 文件类型'), 'the top New menu must expose supported Windows ShellNew file types');
assert(shellNewService.includes("app.getFileIcon(iconSource, { size: 'normal' })") && projectWorkspace.includes('type.iconDataUrl'), 'the top New menu must display Windows-associated file type icons');
assert(read('src/index.css').includes('.project-create-menu .project-menu-item { display:flex; align-items:center; gap:.5rem; }') && projectWorkspace.includes('project-create-menu') && projectWorkspace.includes('重新扫描 Windows 新建文件类型'), 'ShellNew menu rows must place text beside the icon and expose manual refresh');
assert(projectWorkspace.includes('setProgressSetup({ ...makeProgressDraft') && projectWorkspace.includes('void loadProgressFolders();'), 'the progress editor must open from cached state before refreshing progress folders');
assert(settingsFeature.includes('overflow-y-auto overscroll-contain'), 'the settings navigator must scroll when its component list exceeds the viewport');
assert(main.includes('isInternalWorkspaceChange(fileName)') && main.includes("normalized.includes('.photoflow-transcode')") && workspaceIpc.includes("normalized.includes('.photoflow-transcode')") && thumbnailDb.includes('is_internal_transient_media_path'), 'workspace watching, browsing, and media indexing must ignore in-progress transcode files');
assert(workspaceDb.includes('is_internal_workspace_directory') && workspaceDb.includes('DELETE FROM projects WHERE id=?') && workspaceIpc.includes("normalized === '_photoflow_safety_temp'"), 'SD import staging directories must stay hidden and purge any stale catalog entries');
assert(workspaceIpc.includes("project.availability !== 'missing'") && workspaceIpc.includes('missingProjectRetentionMs = 30 * 24 * 60 * 60 * 1000') && workspaceIpc.includes('listMissingProjects(root, Date.now() - missingProjectRetentionMs)') && workspaceDb.includes('purge_missing_project'), 'externally removed projects must disappear from the UI, reconnect within retention, and purge after 30 days');
assert(main.includes("['_photoflow_safety_temp'") || main.includes("'_photoflow_safety_temp'].includes(normalized)"));
assert(systemIpc.includes('suppressWorkspaceWatchPath(targetPath)') && systemIpc.includes('thumbnailService?.syncChangedPaths(targetPath, changedPaths') && !systemIpc.includes('thumbnailService?.scanProject(targetPath'), 'classified imports must suppress transient watcher work and incrementally index only final imported media paths');
assert(!main.includes("project.availability !== 'missing') scheduleMediaTrackingScan(root, project.name)") && !main.includes('await thumbnailService.scanProject(path.join(root, project.relative_path)') && !workspaceIpc.includes('directoryIndex.then(indexed => indexed && thumbnailService.scanProject') && !workspaceIpc.includes("projectName !== '.__photoflow_inspiration__') scheduleMediaTrackingScan"), 'idle reconciliation and restored project tabs must not trigger recursive whole-project media scans');
assert(fileRootWatcherService.includes('binding.onChanged(publishedEntries)') && workspaceIpc.includes('externalTrackingChangeHandler') && workspaceIpc.includes('scheduleMediaTrackingScan(') && workspaceIpc.includes('onChanged: onExternalChanged'), 'managed external watcher events must schedule the same bounded backend tracking scan as project-local changes');
assert(workspaceIpc.includes('requiredRoots: bindings.length') && workspaceIpc.includes('failedRoots') && workspaceIpc.includes('reconciliationFailed') && projectWorkspace.includes('result.degraded === true') && projectWorkspace.includes('{ reconcile: true }'), 'project watchers must report partial external failures, reconcile missed changes, and enable renderer polling fallback');
assert(types.includes('recoveryRequired?: boolean') && projectWorkspace.includes('handleProjectImportRecovery(result)') && projectWorkspace.includes('请勿直接重复导入同一来源'), 'import recovery state must be represented in the renderer contract and refresh the project before preventing duplicate retries');
assert(workspaceIpc.indexOf('const watcherResults = linkedItems.map') < workspaceIpc.indexOf("label: '导入外链'") && workspaceIpc.includes('removeUndoOperation(importUndoToken)'), 'external-link imports must finish watcher setup before publishing undo and precisely remove a late failed record');
assert(importRecovery.includes('cleanupErrors.push({ path: target') && importRecovery.includes('leftoverPaths') && !workspaceIpc.includes('for (const target of createdTargets) await fs.promises.rm(target, { recursive: true, force: true }).catch(() => undefined)'), 'failed import cleanup must report locked leftovers instead of swallowing every removal error');
assert(versionTreeCanvas.includes('mergedAfterConflict') && versionTreeCanvas.includes('latest.positions.forEach') && versionTreeCanvas.includes('historyEpochRef.current += 1') && versionTreeCanvas.includes('requiredHistoryEpoch') && versionTreeCanvas.includes('applyPositions(merged)'), 'stale layout patches must merge winning server positions and invalidate queued history from the losing revision');
const versionManagerSource = read('src/components/VersionManager.tsx');
assert(versionManagerSource.includes('const pageGenerationRef = useRef(0)') && versionManagerSource.includes('pageIdentityRef.current !== pageIdentityKey') && versionManagerSource.includes('selectBranchPhoto(photo.photoId)') && (versionManagerSource.match(/pageGenerationIsCurrent\(pageGeneration\)/g) || []).length >= 12, 'version mutations must be isolated from entry and branch-photo generation changes');
assert(thumbnailPipeline.includes('Directory indexing is metadata-only') && !thumbnailPipeline.includes('pending.push(entry)') && !thumbnailPipeline.includes("state: 'QUEUED' }, 10 * 60 * 1000"), 'directory browsing must not invisibly warm every uncached media thumbnail');
assert(main.includes('app.requestSingleInstanceLock()') && main.includes('thumbnailService.ensureStartupRecovery(startupMediaCacheConfig)')
  && main.includes('await startupRecovery.admitted')
  && main.indexOf('approvedMediaCacheDirectories.add(path.resolve(savedCacheDirectory))') < main.indexOf('thumbnailService.activateStartupRecovery()')
  && main.indexOf('await startupRecovery.admitted') < main.lastIndexOf('loadMainWindowRenderer()'), 'startup cache recovery must be single-instance and await maintenance admission before renderer media requests start');
assert(thumbnailService.includes("const RECOVERY_TYPE = 'thumbnail-cache-recovery'") && thumbnailService.includes('maintenanceKey')
  && thumbnailService.includes("resumePolicy: 'safe-restart'") && thumbnailService.includes('ensureStartupRecovery')
  && thumbnailService.includes("MIGRATION_VERSION = 'thumbnail-cache-migration-v2'")
  && thumbnailService.includes("RECONCILIATION_VERSION = 'thumbnail-reconcile-publications-v1'")
  && thumbnailDb.includes('def run_thumbnail_cache_migration')
  && thumbnailDb.includes('PRAGMA user_version')
  && thumbnailDb.includes('"type": "ready"')
  && thumbnailPipeline.includes("response.type === 'ready'")
  && thumbnailPipeline.includes('migrationCursor = migration.cursor')
  && main.includes('const resolveMediaCacheDir') && thumbnailPipeline.includes('ensureCacheDirectory')
  && thumbnailPipeline.includes('isSafeManagedCachePath') && thumbnailDb.includes('def maintenance_state_complete'), 'startup recovery must be versioned, retryable, and guarded by strict cache-path ownership checks');
assert(mediaCacheNamespace.includes("'.photoflow-cache', installationId.toLocaleLowerCase()")
  && mediaCacheNamespace.includes('loadOrCreateInstallationId')
  && main.includes('configuredDirectory: config.directory')
  && storageUsageService.includes('resolveMediaCacheDirectory(config.mediaCache || {})'),
'custom media caches must isolate every installation beneath its own persistent namespace');
assert(thumbnailDb.includes('generation: str = ""') && thumbnailDb.includes('generation_max_row_id: int = 0')
  && thumbnailDb.includes('after_row_id: int = 0') && thumbnailDb.includes('inspect_limit: int = 2048')
  && thumbnailDb.includes('delete_limit: int = 512') && thumbnailDb.includes('directory_cursor: dict | None = None')
  && thumbnailDb.includes('thumbnail_path IN (') && thumbnailDb.includes('def maintenance_state_save')
  && thumbnailPipeline.includes('generation_max_row_id: Number(recoveryCursor.generationMaxRowId)')
  && thumbnailPipeline.includes('after_row_id: Number(recoveryCursor.afterRowId)')
  && thumbnailService.includes('previousCursor.generation && !Number(previousCursor.lastCompletedAt)')
  && thumbnailService.includes('generationMaxRowId: 0, afterRowId: 0, lastCompletedAt: 0')
  && thumbnailDb.includes('thumbnail_orphan_delete_retries')
  && thumbnailDb.includes('thumbnail_orphan_scan_entries')
  && thumbnailPipeline.includes("record_orphan_delete_failures")
  && !thumbnailPipeline.includes('exclude_paths: [...attempted].slice(-512)')
  && !thumbnailDb.includes('SELECT thumbnail_path FROM thumbnails WHERE cache_root=?'),
'startup thumbnail recovery must page database rows and directories from a persisted cursor without rebuilding the full indexed-path set');
assert(!systemIpc.includes('activeClassifyWrite') && systemIpc.includes("const tracksImportTask = scriptName === 'classify.py' && ['plan', 'import', 'broll'].includes(classifyStage)") && systemIpc.includes("const writesImportFiles = scriptName === 'classify.py' && ['plan', 'import', 'broll'].includes(classifyStage)") && systemIpc.includes('resources: [...(writesImportFiles ? importTargets : []), ...sourcePaths].filter(Boolean)'), 'SD-card planning and its safety staging writes must retain the shared resource-aware project lock');
assert(systemIpc.includes('PYTHON_BACKGROUND_TASK_PROFILES') && systemIpc.includes("'png_to_jpg.py'") && systemIpc.includes("'research.py'") && systemIpc.includes("'ffmpeg_transcode.py'") && systemIpc.includes("'cut_video.py'") && systemIpc.includes('presentationOwnerPageId') && systemIpc.includes('presentationPanelKind') && systemIpc.includes('pythonToolResourcePaths(scriptName, args, path)') && systemIpc.includes("resumePolicy: 'atomic'"), 'shared Python tools must register real path-aware background tasks with restorable panel ownership and honest interruption policy');
assert(toolViews.includes("['--import_session', currentImportSessionRef.current || importRequestIdRef.current]") && toolViews.includes("'--dest_path', resolvedDestinationPath") && classifyImport.includes('load_staged_import(dest_path, import_session)'), 'project routing must copy into a per-card resumable safety area and reuse its manifest before classifying local files');
assert(!toolViews.includes("status === 'decision' || currentStageRef.current === 'plan'") && toolViews.includes("if (status === 'decision')"), 'cancelling an active SD-card planning scan must stop its background worker instead of only resetting the page');
assert(systemIpc.includes('importTargets = [...new Set(routedTargets)]') && systemIpc.includes('for (const targetPath of importWatchSuppressedPaths)'), 'routed SD imports must lock, refresh, and scan the actual project targets instead of the whole workspace');
assert(!app.includes("{activeTab === 'home' && <div") && app.includes("activeTab === 'home' ? 'mx-auto max-w-6xl space-y-4' : 'hidden'"), 'the home dashboard must remain mounted while another tab is active so import planning and batch state survive navigation');
assert(toolViews.includes('hidden={!open}') && !toolViews.includes('{open && <div className={`border-t p-5'), 'collapsing a home panel must hide its body without unmounting active import state or starting a duplicate task when reopened');
assert(toolViews.includes('driveImportSessionsRef') && toolViews.includes('continueAfterDriveFailureRef') && toolViews.includes('retryDrivePathsRef') && toolViews.includes('继续导入 ${nextDrive.path}') && !/设备 \$\{currentDrive\} 已断开[\s\S]{0,500}importQueueRef\.current = \[\]/.test(toolViews), 'a disconnected SD card must retain its resumable staging session, continue the connected-card queue, and retry only unfinished cards');
assert(classifyImport.includes("'skipped': True, 'skipReason': 'no-media'") && toolViews.includes('const skipped = event.data?.skipped === true') && toolViews.includes('没有符合条件的媒体，已跳过；接下来导入'), 'an empty connected SD card must be reported as skipped and advance to the next card instead of failing the batch');
assert(toolViews.includes('可选择同一项目') && !toolViews.includes('selectedProjects.size !== dateGroups.length'), 'adaptive shooting segments must remain independently routable to the same project');
assert(main.includes(".photoflow-workspace-id"), 'workspace databases must use a stable identity marker that survives moving the workspace');
assert(main.includes("'.photoflow-workspace-id']"), 'workspace browsing must hide the internal workspace identity marker');
assert(/show: false,[\s\S]*?once\('ready-to-show'[\s\S]*?mainWindow\.maximize\(\)[\s\S]*?mainWindow\.show\(\)/.test(main), 'the main window must start maximized without flashing its initial bounds');

const electronSources = fs.readdirSync(path.join(root, 'electron'), { recursive: true })
  .filter(name => name.endsWith('.cjs'))
  .map(name => read(path.join('electron', name)))
  .join('\n');
const registeredChannels = new Set([...electronSources.matchAll(/ipcMain\.(?:handle|on)\(\s*['"]([^'"]+)['"]/g)].map(match => match[1]));
const requestedChannels = [...preload.matchAll(/ipcRenderer\.(?:invoke|send)\(\s*['"]([^'"]+)['"]/g)].map(match => match[1]);
for (const channel of requestedChannels) assert(registeredChannels.has(channel), `preload channel is not registered: ${channel}`);

assert(findPluginByCapability('team-retouch.detect')?.id === 'team-retouch');
assert.strictEqual(findPluginByCapability('research.organize'), undefined);
assert.strictEqual(findPluginByCapability('office-media.extract'), undefined);
assert(Object.values(PLUGIN_DEFINITIONS).every(plugin => Array.isArray(plugin.capabilities) && plugin.capabilities.length));

const testBackgroundTasks = async () => {
  const eventBus = createEventBus();
  const service = createBackgroundTaskService({ eventBus, maxHistory: 10 });
  const updates = [];
  eventBus.on('background-task:changed', delta => updates.push(delta));

  const completed = await service.run({ type: 'test', title: 'test task' }, async task => {
    task.report(50, 'half');
    return 42;
  });
  assert.strictEqual(completed.result, 42);
  assert.strictEqual(completed.task.state, 'completed');
  assert(updates.some(delta => delta.upserts.some(task => task.progress === 50)));
  assert(updates.every((delta, index) => index === 0 || delta.revision > updates[index - 1].revision));

  let attempts = 0;
  let retryRun;
  retryRun = () => service.run({ type: 'retry-test', title: 'retry test' }, async () => {
    attempts += 1;
    if (attempts === 1) throw new Error('first attempt fails');
    return 'retried';
  }, retryRun);
  let failedId = '';
  try {
    await retryRun();
  } catch {
    failedId = service.list().find(task => task.type === 'retry-test')?.id || '';
  }
  assert(failedId, 'failed task was not retained');
  const retryAccepted = await service.retry(failedId);
  const retried = await retryAccepted.completion;
  assert.strictEqual(retried.result, 'retried');

  const cancelling = service.run({ type: 'cancel-test', title: 'cancel test' }, task => new Promise(resolve => {
    task.signal.addEventListener('abort', () => resolve('stopped'), { once: true });
  }));
  await new Promise(resolve => setImmediate(resolve));
  const cancelTask = service.list().find(task => task.type === 'cancel-test');
  assert(cancelTask && service.cancel(cancelTask.id));
  const cancelled = await cancelling;
  assert(cancelled.cancelled);
  service.stop();
  eventBus.clear();
};

testBackgroundTasks().then(() => console.log('Architecture contracts passed.')).catch(error => {
  console.error(error);
  process.exitCode = 1;
});
