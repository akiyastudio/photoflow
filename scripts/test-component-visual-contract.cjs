const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=relative=>fs.readFileSync(path.join(root,relative),'utf8');const contract=read('component-sdk/ui.css');const hostStyle=read('src/index.css');
for(const token of ['canvas','surface','subtle','elevated','border','text','muted','primary','focus','success','warn','danger','radius-sm','shadow-sm','space-3','control-md'])assert(contract.includes(`--pf-${token}:`),`shared UI contract missing --pf-${token}`);
for(const primitive of ['.pf-canvas','.pf-surface','.pf-card','.pf-toolbar','.pf-modal','.pf-button','.pf-input','.pf-banner','.pf-status'])assert(contract.includes(primitive),`shared UI primitive missing ${primitive}`);
assert(hostStyle.includes("@import '../component-sdk/ui.css'"));assert(contract.includes('prefers-reduced-motion')&&contract.includes(':focus-visible'));console.log('Component visual contract tests passed');
