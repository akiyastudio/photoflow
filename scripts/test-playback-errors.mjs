import assert from'node:assert/strict';import{classifyPlaybackError,PlaybackFailure}from'../src/contracts/playback-errors.ts';assert.equal(classifyPlaybackError(new Error('GPU device lost')).code,'GPU_DEVICE_LOST');assert.equal(classifyPlaybackError(new Error('surface HWND invalid')).code,'SURFACE_LOST');assert.equal(classifyPlaybackError(new Error('startup timeout')).code,'START_TIMEOUT');assert.equal(classifyPlaybackError(new Error('用户取消')).code,'CANCELLED');const failure=new PlaybackFailure('MEDIA_UNSUPPORTED','codec',true,[{backendId:'a',phase:'start',startedAt:1,endedAt:2,errorCode:'MEDIA_UNSUPPORTED',automatic:true}]);assert.equal(classifyPlaybackError(failure),failure);console.log('Playback error code and attempt classification tests passed.');
