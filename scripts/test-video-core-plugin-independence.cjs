const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),{spawnSync}=require('node:child_process');const root=path.resolve(__dirname,'..'),source=path.join(root,'plugins','video-playback-backend'),hidden=path.join(root,'plugins',`.video-playback-backend.hidden-core-test-${process.pid}`);assert.equal(path.dirname(source),path.join(root,'plugins'));assert.equal(path.dirname(hidden),path.join(root,'plugins'));if(!fs.existsSync(source))throw new Error('plugin source missing before independence test');
const run=(command,args)=>{const result=spawnSync(command,args,{cwd:root,encoding:'utf8',windowsHide:true});if(result.status!==0)throw new Error(result.stderr||result.stdout||`${args.join(' ')} failed`);process.stdout.write(result.stdout||'');};
try{fs.renameSync(source,hidden);for(const test of ['scripts/test-video-playback-architecture.cjs','scripts/test-video-playback-host-security.cjs','scripts/test-video-playback-broker.cjs'])run(process.execPath,[test]);run(process.execPath,[path.join(root,'node_modules','typescript','bin','tsc'),'-b','--pretty','false']);console.log('Core playback compile and boundary tests pass without plugin source.');}finally{if(fs.existsSync(hidden)&&!fs.existsSync(source))fs.renameSync(hidden,source);}
