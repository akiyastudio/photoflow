import { useTeamOutputProgress } from './useTeamOutputProgress';

export const TeamOutputProgressPicker = ({ controller, disabled }: { controller: ReturnType<typeof useTeamOutputProgress>; disabled: boolean }) => <label className="flex min-w-0 items-center gap-2 text-xs text-slate-500"><span className="shrink-0 font-bold text-slate-600">合成保存到</span><select aria-label="合成结果目标进度" value={controller.targetProgressId} disabled={disabled} onChange={event => controller.setTargetProgressId(event.target.value)} className="min-w-48 max-w-72 rounded-md border border-slate-200 bg-white px-2.5 py-2 text-xs font-bold text-slate-700 disabled:opacity-50"><option value="__new__">新建下一图片进度（推荐）</option>{controller.folders.map(folder => <option key={folder.id} value={folder.id}>V{folder.versionKey} · {folder.displayName}</option>)}</select><span className="hidden max-w-64 truncate text-slate-400 xl:inline">同一来源进度会记住此选择</span></label>;
