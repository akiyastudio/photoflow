export const PLAYBACK_ERROR_CODES=['BACKEND_UNAVAILABLE','UNSUPPORTED_CONTAINER','UNSUPPORTED_CODEC','DECODE_INITIALIZATION_FAILED','HARDWARE_DECODING_FAILED','RENDER_INITIALIZATION_FAILED','SURFACE_LOST','GPU_DEVICE_LOST','CORRUPT_MEDIA','MEDIA_IO_FAILED','PERMISSION_DENIED','STARTUP_TIMEOUT','CANCELLED','BACKEND_CRASHED']as const;export type PlaybackErrorCode=typeof PLAYBACK_ERROR_CODES[number];export type PlaybackFallbackSuggestion='chromium'|'component'|'system-player'|'none';export type PlaybackAttempt={backendId:string;phase:'probe'|'start'|'runtime'|'release';startedAt:number;endedAt:number;errorCode?:PlaybackErrorCode;message?:string;automatic:boolean};export type PlaybackResult<T>={success:true;value:T;attempts:PlaybackAttempt[]}|{success:false;error:{code:PlaybackErrorCode;message:string;recoverable:boolean;suggestedFallback:PlaybackFallbackSuggestion};attempts:PlaybackAttempt[]};
const LEGACY:Record<string,PlaybackErrorCode>={START_TIMEOUT:'STARTUP_TIMEOUT',MEDIA_UNSUPPORTED:'UNSUPPORTED_CODEC',DECODE_FAILED:'DECODE_INITIALIZATION_FAILED',INPUT_REVOKED:'PERMISSION_DENIED',CAPTURE_FAILED:'MEDIA_IO_FAILED',RESOURCE_RELEASE_FAILED:'BACKEND_CRASHED',ALL_BACKENDS_FAILED:'BACKEND_UNAVAILABLE'};const ERROR_CODE_SET:ReadonlySet<string>=new Set(PLAYBACK_ERROR_CODES);const stableCode=(value:unknown):PlaybackErrorCode|undefined=>{const raw=String(value||'');const mapped=LEGACY[raw]||raw;return ERROR_CODE_SET.has(mapped)?mapped as PlaybackErrorCode:undefined;};export class PlaybackFailure extends Error{readonly code:PlaybackErrorCode;readonly recoverable:boolean;readonly suggestedFallback:PlaybackFallbackSuggestion;readonly attempts:PlaybackAttempt[];constructor(code:PlaybackErrorCode|string,message:string,recoverable=true,attempts:PlaybackAttempt[]=[],suggestedFallback:PlaybackFallbackSuggestion='component'){super(message);this.name='PlaybackFailure';this.code=stableCode(code)||classifyPlaybackError(message).code;this.recoverable=recoverable;this.attempts=attempts;this.suggestedFallback=suggestedFallback;}}
export const classifyPlaybackError=(value:unknown,fallback:PlaybackErrorCode='DECODE_INITIALIZATION_FAILED'):PlaybackFailure=>{if(value instanceof PlaybackFailure)return value;const raw=value as{code?:string;message?:string},message=value instanceof Error?value.message:String(raw?.message||value||'视频播放失败'),text=message.toLowerCase();let code=stableCode(raw?.code);if(!code)code=/cancel|取消|替换|关闭/.test(text)?'CANCELLED':/timeout|超时/.test(text)?'STARTUP_TIMEOUT':/permission|access denied|拒绝访问|授权/.test(text)?'PERMISSION_DENIED':/hardware decode|hwdec|硬件解码/.test(text)?'HARDWARE_DECODING_FAILED':/render initialization|render init|渲染初始化/.test(text)?'RENDER_INITIALIZATION_FAILED':/gpu|device lost|d3d/.test(text)?'GPU_DEVICE_LOST':/surface|表面|hwnd/.test(text)?'SURFACE_LOST':/container|容器/.test(text)?'UNSUPPORTED_CONTAINER':/codec|编码/.test(text)?'UNSUPPORTED_CODEC':/corrupt|损坏/.test(text)?'CORRUPT_MEDIA':/io|读取|文件/.test(text)?'MEDIA_IO_FAILED':/missing|未安装|不可用/.test(text)?'BACKEND_UNAVAILABLE':fallback;const recoverable=!['CANCELLED','CORRUPT_MEDIA','PERMISSION_DENIED'].includes(code);const suggestion:PlaybackFallbackSuggestion=code==='CANCELLED'?'none':code==='CORRUPT_MEDIA'||code==='PERMISSION_DENIED'?'system-player':'component';return new PlaybackFailure(code,message,recoverable,[],suggestion);};
